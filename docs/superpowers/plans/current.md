# Current Plan State

Last updated: 2026-07-22.

## Repo State

- Frontend motion and rendering polish is complete on local `dev` (details: `iterations/2026-07-22-frontend-motion-rendering-polish.md`). Theme switching now runs through `document.startViewTransition` when supported (instant fallback otherwise), the Loading screen gained an aura/wave/shimmer treatment, and route-level dynamic-import fallbacks now share it through a `RouteLoading` wrapper (160 ms anti-flash delay + `fade-in` entrance). IconButton and sidebar nav items gained spring press/lift micro-interactions, the message-tail entrance uses a new `message-enter` settle curve, message action rails animate their 4px reveal instead of jumping, and `.chat-message-row` uses `content-visibility: auto` with `contain-intrinsic-size: auto 140px` so offscreen rows in long conversations skip rendering while anchor paging math stays intact. `::selection` and scrollbar hover are themed for both light and dark. All new motion is gated by `prefers-reduced-motion` and `@media (hover: hover)`; no provider/store/MCP/auth/persistence/API/deployment/Tauri/dependency semantics changed. Verification: full Jest `87/87` suites (`869/869`), Gemini visual contract `85/85` (one transition contract regex intentionally updated for the action-rail transform addition), zero-warning ESLint, TypeScript, `git diff --check`, production build (`/` 108 kB, `195 kB` First Load JS), and WebBridge Browser QA on a credential-free local dev instance: 240-message paging 15->30->45 capped and anchored, zero overflow at desktop/`390x844`, light+dark screenshots clean, zero interaction console errors, real Settings theme switches invoking `startViewTransition` with correct body classes, and throttled-network route navigation showing the shared loading dots/shimmer with fast-path no-flash behavior. A third slice then unified all overlay entrances onto the same language: shared `overlay-panel-in`/`overlay-sheet-in` keyframes plus the existing `fade-in` backdrop now drive the update announcement (its three local keyframes deleted), every `showConfirm`/`showPrompt`/Modal via `.modal-mask` (new 0.18s fade) + `.modal-container` (settle curve, with a bottom-sheet `overlay-sheet-in` variant under 600px); the reduced-motion fallback for `.modal-mask` was repaired with a dedicated valid rule after discovering the grouped selector was silently dropped in Chrome (pre-existing `::-ms-thumb`/`::-moz-range-thumb` invalidation — broader revival stays out of scope). Same verification battery passed (`87/87`, `869/869`, contract `85/85` with intentionally repointed keyframe assertions) plus WebBridge QA of announcement/confirm in both themes, 390px sheet emulation, and reduced-motion emulation.
- The requirements-first Composer follow-up is complete on local `dev`; the product diff and this handoff ship in the same local commit. The external remediation requirements and all 19 reference images remain the only visual authority, and the superseded clause that allowed Add or other controls to remain visually independent is still discarded. This pass closes the audited Hero/Composer geometry, `8px` Compact padding, `44px` hit versus `40px` Model optical surface, semantic Model-name accent, Rail Surface/Border/Shadow, Mobile `56dvh` cap, Tools/Mode typography, page-wash intensity, and short-landscape collision gaps without changing the Composer DOM or business chain. Empty Composer now has one root VisualViewport-clamped positioning authority plus only the necessary Mobile/Desktop responsive variants; the obsolete collapsed, segment, and file-tail duplicates were removed and are rejected by tests.
- Final deterministic verification passed 13 Composer suites (`92/92` tests), the Gemini visual contract (`85/85` tests), complete Jest (`87/87` suites, `862/862` tests), zero-warning ESLint, TypeScript, `git diff --check`, and production build (`/` 108 kB, `194 kB` First Load JS). The final diff adds no dependency and touches no Provider, Store, request, attachment, auth, API, deployment, or Tauri contract.
- In-app Browser QA captured the mathematically complete 52-state required viewport/theme matrix plus 5 posture cases, 4 exact-size native-reference extras, 2 Focus states, and a `390x420` soft-keyboard proxy: 64 measured screenshots, followed by 2 orientation-retention screenshots and one `320x700` Extra High Chip proof. Across the 64 measured cases there was zero horizontal overflow, no Shell or visible dialog left the viewport/segment, every Compact Shell measured `64px`, all Model/parameter layers stayed at or below `460px` desktop width and `viewport - 20px` mobile width, all above-Shell panels kept a `12px` gap, and 192 measured core controls retained `44px` height. Every one of the 19 references has an exact-viewport side-by-side comparison under `/tmp/neatchat-composer-remediation-followup/`.
- Rotation QA exposed and closed one follow-up defect: at `932x430`, an Expanded empty Composer previously ended `13.5px` below the VisualViewport. The short-wide rule now bottom-anchors it at `418/430`; a Dark Reasoning panel repositions to `103.75..342px` with a `12px` Shell gap, while the mixed two-line draft and three attachments survive `430x932 -> 932x430`. The `390x420` Reasoning proxy measured a `235.2px` panel cap, and a real model switch proved `Extra High` remains fully visible at `320px` while only the model name truncates. Browser logs contained no warning/error.
- Preserved boundaries are unchanged: real model/provider/config data, request payloads, streaming and Stop, attachment parsing/limits, Prompt Store data, auth/access, persistence, MCP/Plugin, API, deployment, Tauri, and dependencies. No credential, real Provider request, physical fold/keyboard/device, Safari/Firefox/Edge run, push, PR, deploy, dependency change, or destructive Git action is claimed. Those external boundaries do not block the deterministic visual sign-off and are not mislabeled as PASS.
- Composer 2.2 Slices 1–6 are complete on local `dev`. Slice 6 starts from `c156b41a` and closes the final independent-review gaps without changing the provider/API/store contracts: opening Tools is now side-effect free; image attachments block an unsafe switch to a non-vision model; Composer actions use a stable localized live-status channel; file cards expose real compact type/size metadata; Prompt Library semantics match its mounted region; capability QA states select a real available model before opening; and Safe Area observation plus Compact/Expanded paint use one cancellable rAF/surface path. Model Chip and Prompt filters now retain `44px` touch geometry, ellipsis, and semantic Composer tokens.
- Final Browser QA passed the existing 26-case viewport/theme matrix plus the frozen Slice 6 spot checks. Desktop `1440x900` covered Tools with an existing image, Models, real Prompt Store data, Reasoning, and Image options. A freshly restarted page covered `390x844` Reasoning, `320x740` full attachments, and `1000x1050` Tabletop Dark: no page overflow, no panel clipping, `44px` Chip/Add/Send targets, and real `MD/JSON/CSV/TS/TXT` file metadata. The Tabletop implementation and `designs/17-tabletop-dark-reasoning.png` were inspected together at the same viewport; Composer hierarchy, lower-segment placement, Rail, and density matched, while the existing sidebar/conversation chrome and real configured data were intentionally preserved. Post-restart Browser warning/error count was zero.
- Final frozen-tree verification passed zero-warning ESLint, TypeScript, complete Jest (`87/87` suites, `850/850` tests), `git diff --check`, production build (`/` 107 kB, `194 kB` First Load JS), and an independent read-only re-review with no P1/P2/P3. Prior Composer commits were already present on `origin/dev` before this follow-up; Slice 6 is kept as one new local commit only. Safari/Firefox/Edge, actual `200%` browser zoom, physical fold/keyboard/touch/pen, screen-reader speech, Voice/microphone, real file dialogs, Provider calls, credentials, push, PR, deploy, dependency changes, and destructive Git remain explicit external boundaries rather than inferred PASS results.
- Composer 2.2 Slice 3 is complete on local `dev` from the committed Slice 2 baseline `52381f22`. The Model Chip now has the explicit `closed / reasoning / image-options / models` contract, shows only the current parameter summary while closed, reveals the real model name while open, uses `currentColor` SVG caret/check/stop icons, and exposes model, Provider, and detail through one localized Label/Title. Escape returns `models -> current parameter layer -> closed`; outside close restores Chip focus. New-chat Chat/Image tabs remain pointer- and keyboard-reachable while the menu is open, participate in the same focus loop, and transfer directly to the next mode's real parameter layer. Models without adjustable parameters no longer expose or re-enter an empty parameter panel.
- Slice 3 preserves the real `useAllModels` Availability/Provider collection, existing model/provider/config-source writes, allowlists, discrete Rail Pointer Capture/keyboard/ARIA, and current-session abort path. Model locks now include both server locked fields and persisted model metadata, disable Switch/model rows, and expose the existing locked reason. Send is one `44px` control with deterministic Disabled/Send/Stop states; Stop only aborts streaming messages in the current session. Voice renders no dead slot and only appears when both existing Realtime feature gates are enabled. Desktop Model/parameter panels measure `500px`; compact panels clamp to the Composer/viewport (`360px` at `390x844`, `296px` at `320x740`). Complete Jest passed `86/86` suites and `829/829` tests; the final focused/visual gate passed `7/7` suites and `119/119` tests; ESLint, TypeScript, `git diff --check`, production build (`/` 107 kB, `193 kB` First Load JS), and independent review all passed. Real Browser QA covered `1440x1024`, `390x844`, `320x740`, Chat/Image layers, mouse and keyboard mode switching, nested Escape/focus return, Send disabled/ready, `currentColor` SVG rendering, and zero horizontal overflow. A fresh post-HMR tab reproduced no Console warning/error. No real Provider, microphone, credential, push, PR, deploy, dependency, or destructive Git action was used. Slice 4 remains the unified collision/tokens/Aura/Container Query/Safe Area/fold pass.
- Composer 2.2 Slice 2 is complete on local `dev` from the committed Slice 1 baseline `8ae07a72`. Tools now has real Upload image, Upload file, and Prompt Library entries; typed chooser accept/filtering reuses the existing attachment pipeline and independent three-image/five-file capacity; Prompt Library searches and filters the real Prompt Store, reuses existing prompt/command insertion, and implements depth-aware Escape/focus return. Tools and Model now switch mutually in one physical click in both directions, including through the model backdrop. Desktop Tools is `268px` with `52px` rows; compact is capped at `280px`; Prompt Library is scroll-bounded to `380px`. Provider/model writes, attachment wire format, slot authority, prompt persistence, submission/Stop, auth, API, MCP, dependencies, and deployment are unchanged.
- Slice 2 verification passed focused Tools/attachment/prompt Jest (`3/3` suites, `25/25` tests), the complete visual contract (`85/85` tests), complete Jest (`84/84` suites, `820/820` tests), zero-warning ESLint, TypeScript, `git diff --check`, and production build (`/` 107 kB, `193 kB` First Load JS). The final accessibility correction also passed `2/2` focused suites and `90/90` tests plus an independent read-only re-review with no commit blocker. Real in-app Browser QA passed `1440x1024`, `390x844`, and `320x740` with no horizontal overflow or framework overlay; it covered three `52px` primary actions, real Prompt records/search/selection, Search focus, nested Escape focus return, Prompt bounds, and bidirectional one-click Tools/Model switching. No real file, credentials, provider request, push, PR, deploy, dependency, or destructive Git action was used. Unified collision placement and the full theme/reduced-motion/fold/cross-browser matrix remain Slices 4–5.
- Composer 2.2 is being delivered on local `dev` in independently verified commits, starting from `962996fa`. Slice 1 replaces the split compact/expanded geometry with one responsive Grid Shell, derives Compact/Expanded/Scrolling from the real textarea `scrollHeight`, ignores wrapped placeholder height for an empty draft, caps content at `min(174px, 24dvh)`, keeps attachment/status content in normal flow, and freezes textarea shape while a menu temporarily reallocates width. The configured `12–40px` font range now resolves a proportional `1.5` line box with a `24px` floor instead of a fixed `24px` line height. Model/provider selection, upload parsing and limits, submission/Stop behavior, IME, unfinished drafts, persistence, request payloads, and Safe Area behavior remain unchanged in this slice.
- Slice 1 final verification passed the complete Jest suite (`83/83` suites, `816/816` tests), the corrected focused contracts (`4/4` suites, `106/106` tests), zero-warning ESLint, TypeScript, `git diff --check`, production build (`/` 106 kB, `193 kB` First Load JS), and independent read-only re-review. Real in-app Browser QA covered `1440x1024`, `390x844`, and `320x740`: empty and one-line drafts stayed `64px` Compact; multiline drafts expanded continuously; long drafts capped at `174px` with internal scrolling; opening the model menu did not change the long-draft height; attachment removal triggered a fresh measurement without clipping; model/Add/Send remained visible; checked views had zero page overflow. Default computed input line height was `24px`; the `40px` boundary is covered by the CSS/test contract and remains a later full-matrix Browser item. Physical mobile keyboard, true fold posture, Safari, Firefox, Edge, and external provider calls are not claimed. No push, PR, deploy, credential entry, provider request, external write, dependency, or destructive Git action was performed.
- The current uncommitted Browser-annotation slice is based on `dev` at `e3238eb1`. Opening the compact model selector elevates the real composer trigger above the modal backdrop, blurs/dims only the non-source composer controls, and suppresses overlapping quick-jump chrome. The backdrop/dialog now mount only while open, so outside-click closure removes them immediately and restores trigger focus. The Retry SVG no longer uses duplicate document-global fragment IDs, preventing the icon from disappearing when the selector mounts. Desktop and mobile message actions now share one transparent, borderless, shadow-free visual language while retaining `34px` desktop and `44px` mobile controls. Mobile Settings uses `10px` safe-area-aware gutters and tighter section headers.
- Fresh verification passed the responsive/a11y contracts (`4/4` suites, `100/100` tests), chat rendering (`2/2` suites, `12/12` tests), zero-warning ESLint, TypeScript, `git diff --check`, and the production build. Real in-app Browser QA covered `487x998`, `430x932`, `320x740`, and `1440x1024`: Retry kept three SVG paths with no fragment IDs across selector open/close; backdrop/dialog were absent from the first post-close sample through about `1.22s`; focus returned to the model trigger; desktop/mobile action rails were transparent with no border/shadow; Settings measured `406px` at `430px` and `296px` at `320px`; all checked views had zero page overflow. The restarted page had zero Browser warning/error and no framework overlay; `/` returned HTML `200` and `/api/config` returned JSON `200`. No commit, push, PR, deploy, provider request, external MCP call, or remote write was performed.
- The final pre-commit review was anchored on the live `dev=origin/dev=b5a768bf` baseline plus the accepted working tree. There is no open or closed GitHub PR for `dev`, so no review thread exists to address. Three independent read-only reviews covered frontend interaction/accessibility, MCP/API/security, and state/migration/test contracts; all returned `PASS` after Root fixes.
- The review closed four deterministic code gaps: Mask search results now derive from the current language-filtered catalog on every render; New Chat filters locale only for built-in Masks and keeps cross-language user Masks visible; the custom Markdown table scrollbar and single message-image frame now opt into `data-chat-horizontal-scroll`, so the mobile left-edge history gesture yields to those horizontal interactions. The Browser acceptance handoff now consistently records ambiguous legacy `language: undefined` state as migrating to Follow App Language.
- Fresh final verification passed complete Jest (`82/82` suites, `806/806` tests), zero-warning ESLint, TypeScript, `git diff --check`, targeted Prettier, and the production build (`/` 107 kB, 193 kB First Load JS). Local runtime smoke returned HTML `200` for `/` and valid JSON `200` for `/api/config`.
- The in-app Browser was available for this final pass and supersedes the earlier broken-runtime boundary. Desktop `1280px`, mobile `390x844`, and narrow `320x740` checks had zero page overflow, zero relevant Console warning/error, and no framework overlay. Verified interactions include closed/open model progressive disclosure, new-chat draft retention, Mask search plus app/all/fixed language switching, mobile chat drawer and chat settings, wide-table rendering/markers, and non-overlapping `320px` composer controls. Physical touch gesture input, real external MCP/provider calls, and paid image generation were not executed; deterministic gesture and integration tests cover the repository boundary.
- The latest Mask migration follow-up closes two data-preservation findings from the final dirty-worktree review. A v3.2 store with the historical default `language: undefined` now migrates to Follow App Language, while an explicit concrete language remains fixed. Retired built-in context is removed only when its legacy ID, role, and content fingerprint all match the canonical preset; user-edited content that retains an old ID is preserved for both global-sync and explicit-model sessions.
- Fresh verification passed the six focused Mask/MCP/config suites (`119/119` tests), complete Jest (`82/82` suites, `805/805` tests), zero-warning ESLint, TypeScript, targeted Prettier, `git diff --check`, production build, and an independent read-only review. All 16 fingerprints were independently recomputed from the canonical `HEAD` mask sources with no mismatch. This is a deterministic persisted-state migration correction with no new UI geometry, so Browser QA is not required for this follow-up. No commit, push, PR, deploy, credential entry, provider request, external MCP call, or remote write was performed.
- The final dirty-worktree repair closes two review findings. Chat persistence now tracks accepted snapshot generations separately from destructive write generations, so an older write that exhausts retries cannot requeue after a newer snapshot has already been accepted and flush the state backward. HTML artifact consent now transfers keyboard focus from Run to the approved iframe, restores focus to the replacement Run control only when the preview owned focus across a code/execution identity change, and leaves pointer or external focus undisturbed. Existing retry limits, terminal error reporting, remove/suspend semantics, artifact approval, CSP, sandbox, and Reload behavior are unchanged.
- Fresh verification passed the three focused persistence/artifact suites (`24/24` tests), complete Jest (`82/82` suites, `805/805` tests), zero-warning ESLint, TypeScript, `git diff --check`, targeted Prettier, production build, and an independent read-only review. A direct race probe records writes as `[1, 2]` instead of the prior `[1, 2, 1]`. The restarted local server is available at `http://localhost:3000`; interactive iframe focus QA remains `需要验证` because the in-app Browser runtime still fails setup after a clean reset with `Cannot redefine property: process`, so deterministic RTL focus coverage is not presented as a real-Browser PASS. No commit, push, PR, deploy, credential entry, provider request, external MCP call, or remote write was performed.
- The final dirty-worktree MCP review finding is closed: tool `inputSchema` validation now follows the MCP SDK contract by treating schemas without `$schema` as JSON Schema 2020-12, dispatching explicit canonical 2020-12 and draft-07 schemas to matching isolated Ajv validators, and rejecting unknown dialects or any schema that compiles to an Ajv async validator before `executeRequest`. The existing WeakMap cache, per-schema `$id` isolation, payload limits, authorization, and generic MCP execution flow are unchanged. Ajv 8.20.0 and `ajv-formats` 3.0.1 are now direct dependencies because application code imports them; both versions were already present through MCP SDK 1.29.0. Fresh verification passed the five MCP suites (`50/50` tests), complete Jest (`82/82` suites, `802/802` tests), zero-warning ESLint, TypeScript, `git diff --check`, Prettier for the touched files, and production build. This is server-side validation work with no UI surface, so Browser QA is not required for this follow-up.
- The final dirty-worktree follow-up closes the remaining mobile gesture P2: Chat history edge navigation now relies on one `data-chat-horizontal-scroll` contract instead of a Markdown class whitelist. Display formulas, code blocks, table viewports (including CSV-style tables), Markdown image frames, Mermaid previews/source/fallbacks, and the existing message gallery all opt into that contract, so a horizontal drag that starts on those surfaces cannot navigate away from the conversation. The ordinary left-edge history gesture, vertical intent threshold, composer attachment strip, provider requests, state, persistence, and rendering behavior are unchanged.
- Fresh verification passed five focused Markdown/gesture suites (`113/113` tests), three adjacent scroll/render suites (`16/16` tests), the complete Jest suite (`82/82` suites, `797/797` tests), zero-warning ESLint, TypeScript, `git diff --check`, and production build. The post-build stale dev server returned HTML `500` until the documented restart; after restart `/` returned HTML `200` and `/api/config` returned valid JSON `200`. Interactive `390x844` gesture QA remains `需要验证`: two in-app Browser connection attempts, including a clean runtime reset, failed with `Cannot redefine property: process`, so no live swipe PASS is inferred from deterministic tests.
- Final dirty-worktree review fixes at `dev=origin/dev=b5a768bf` close the remaining five findings without expanding into the deferred P2/P3 backlog. MCP JSON Schema validators are isolated per schema identity, so duplicate third-party `$id` values cannot pollute another tool. Mask migration removes only the exact retired built-in context IDs and preserves user Pin order. Successful MCP results now have an explicit untrusted-data continuation contract and are always JSON-serialized, preventing string results from escaping their response fence. Artifact consent and preview share one computed height, while Reload stays disabled until the current code instance is approved and resets after code or artifact changes.
- Retired `jimeng-mcp` configuration is now tombstoned at MCP read and mutation boundaries, excluded from admin/chat/runtime projections, initialization, and subsequent writes, and rejected if re-added. Initialization and full restart synchronously remove any stale same-ID runtime from the executable registry, then close its transport best-effort without allowing a pending or failed close to block generic MCP. `.env.template` now matches the server contract: only explicit `ENABLE_MCP=true` enables generic MCP, and legacy token presence does not auto-enable it.
- Final verification passed seven focused suites / `54` tests (`artifacts-preview`, artifact route, built-in masks, MCP display/chat/config/initialization), the complete Jest suite (`82/82` suites, `797/797` tests), zero-warning ESLint, TypeScript, `git diff --check`, production build, local HTTP smoke, and an independent read-only review after one bounded MCP fence-escape correction. `/` returned HTML `200`; `/api/config` returned valid JSON `200`. Interactive desktop/mobile Browser QA remains `需要验证` because the in-app Browser runtime fails setup with `Cannot redefine property: process`; no visual PASS is inferred from code or tests. No commit, push, PR, deploy, credential entry, provider request, external MCP call, or remote configuration write was performed.
- Latest local acceptance work closes the original six Browser regressions plus the second mobile follow-up without reopening the broader P2/P3 backlog. New-chat drafts keep the welcome hero and centered composer until submit; composer focus no longer moves a reader; closed model controls reveal only the active reasoning/image detail; the mobile empty composer now uses the available width instead of a `376px` cap while retaining the protected image-parameter lane.
- The Mask catalog follows app language by default and now uses a compact horizontal row at `421–600px`, an explicit stacked layout at `≤420px`, and full-width filter controls at `≤358px`. Its subtitle class is corrected and header actions have accessible names. Mobile and desktop sidebars share the same Settings row; the mobile-only `NeatChat / Local` account card and its unreachable CSS/tokens are removed.
- The quick-jump wash and duplicate waiting skeleton remain removed. The localized Typing status now sits in the same centered `780px` prose track as the assistant header, while stream reveal/caret and Reduced Motion remain. The error boundary keeps data export/reset recovery but no longer renders the external `Report This Error` action.
- Final verification for the combined follow-up passed `81/81` Jest suites (`784/784` tests), the latest focused six-suite set (`114/114` tests), zero-warning ESLint, TypeScript, `git diff --check`, production build, and independent read-only review. The local dev server was restarted after the build and remains available at `http://localhost:3000`. Post-fix interactive Browser automation remains `需要验证` because the in-app Browser runtime still fails setup with `Cannot redefine property: process`; no visual PASS is inferred from code or tests.
- The accepted slice included in this commit starts from `dev=origin/dev=b5a768bf` (`main=0280cb3d`, `main...dev=0/50`) and supersedes the prior Jimeng repair work by removing the Jimeng MCP integration in full. Its dedicated runtime, API proxy, default config, environment fallback, action/menu/toggle/state, polling and recovery paths, localized copy, public catalog entries, and Jimeng-only tests are gone. Native OpenAI/Azure image generation and editing, the generic MCP market/runtime, and the generic message image gallery remain available.
- The remaining merge-blocking P1 work is closed in the same slice: chat requests capture Plugin IDs and Responses recovery state before asynchronous preparation; persistence uses bounded retry with generation guards and a visible terminal error; HTML artifacts require explicit execution consent and inject a restrictive CSP before user markup; artifact route fetches are abort-safe; mobile history swipe begins only from the left edge and yields to vertical or horizontally scrollable content; generic MCP validates discovered tools and JSON Schema before executing, rejects oversized/deep/prototype-polluting payloads, and keeps private upstream errors out of user-visible messages.
- Built-in masks are reduced to eight capability families per locale with stable string IDs, concise task contracts, current global model configuration, locale-first discovery, explicit aliases for useful legacy IDs, tombstones for obsolete presets, and a migration that preserves user-authored masks and safe built-in context while removing obsolete model overrides.
- Verification passed the complete Jest suite (`81/81` suites, `783/783` tests), the complete Gemini visual contract (`84/84`), zero-warning ESLint, TypeScript, `git diff --check`, mask generation, production build, and an independent read-only P1 review. Local HTTP smoke returned HTML `200` for `/` and valid JSON `200` for `/api/config`. Interactive desktop/mobile Browser QA remains `需要验证`: the in-app Browser runtime failed during setup with `Cannot redefine property: process`, so no visual PASS is claimed from that tool failure.
- This commit contains only the accepted repository slice. No PR, deploy, paid provider request, external MCP call, credential entry, or remote configuration write is included. Existing deployment- or user-owned runtime MCP configuration is outside the repository snapshot; any legacy removed server entry there must be checked and deleted before rollout.
- Current completed local slice starts from `dev=origin/dev=7770933e` and repairs the upgraded Jimeng MCP contract without changing its endpoint or Secret handling. Explicit Jimeng activation now serially detaches any stale process-global client, reconnects through the existing Streamable HTTP transport, refreshes `tools/list`, requires at least 17 discovered `dreamina_*` tools, and calls the read-only `dreamina_version` tool before exposing the runtime. Failed refreshes remove the transport and tool snapshot instead of falling back to the old seven-tool cache; generic MCP initialization/reuse and persisted paused state remain unchanged.
- The Jimeng request guard now forces all eight currently discovered submit tools to `poll=0`. Explicit `1k` is preserved only for `dreamina_text2image` with model `3.0`/`3.1`; default/new text-to-image, image-to-image, and image-upscale requests normalize an invalid `1k` to `2k`, while valid `2k`/`4k`, omitted resolution, video resolution, query, account, and session tools remain untouched. The system prompt carries the same rule while the real tool names and schemas continue to come from dynamic discovery.
- The image-generation action shows a disabled `aria-busy` loading state with localized enabling/disabling text while activation settles, so reconnect latency is visible and repeated clicks cannot silently queue. Desktop and `390x844` Chrome QA confirmed the pending state, successful disable/re-enable, automatic mobile menu close only after success, zero page overflow, and no browser warning/error.
- Verification passed the complete Jest suite (`81/81` suites, `835/835` tests), 6 targeted suites / 175 tests, zero-warning ESLint, TypeScript, `git diff --check`, and a production build. An authorized read-only live smoke returned exactly 17 `dreamina_*` tools and successfully called `dreamina_version`; local Browser QA performed three real reconnects without submitting a paid image/video task. No Secret value was printed or persisted, and no push, PR, deploy, provider generation, or remote configuration write occurred.
- Latest completed slice: comprehensive `origin/main...origin/dev` frontend performance, compatibility, and experience review at `main=0280cb3d`, baseline `dev=ed5bac80`, delivered as `32973c0e` and pushed to `origin/dev`. The baseline range contained 47 non-merge commits and 207 changed files (`+35,023/-7,334`). No merge, PR, deploy, rebase, history rewrite, `main` checkout, or `main` push occurred.
- Long-chat work keeps the normal first paint at the latest 15 messages and a hard 45-message DOM window, backfills only for underfilled viewports, and shifts only when a visible anchor survives the next window. Long streaming Markdown now coalesces at 120/250/500/1200 ms beyond 32K/64K/128K/256K, token estimates no longer allocate a large temporary array, and duplicate scroll/ResizeObserver/listener/localStorage work was removed.
- Cross-layer fixes also preserve Jimeng video/opaque public media, stop successful queries from turning into 120-second timeouts, keep the local image-generation toggle from deactivating the process-global MCP client, and recursively merge the latest sync state over nested defaults without prototype-pollution keys.
- Apple-style motion intent remains: the reasoning rail keeps its sparkle and easing but animates the fill with `scaleX`; the last-message entrance only applies to the real tail and is disabled for Reduced Motion. Safari glass surfaces include `-webkit-backdrop-filter`; compact textarea text is at least 16px; mobile header/message/code actions use 44px targets; Settings is a single semantic link.
- Final committed-state verification passed `81/81` Jest suites (`823/823` tests), zero-warning ESLint, TypeScript, `git diff --check`, production build (`/` 108 kB, 194 kB First Load JS), and Chrome long-chat QA. A 240-message mixed fixture rendered 15 messages at the tail, grew only to 30/45 while paging, returned to 15 at the latest edge, kept document overflow at 0, accepted 280 typed characters in about 15 ms, and reported no browser warning/error. Light and Dark were both checked.
- Physical Safari, Firefox, Edge, Windows High Contrast, and a final post-fix mobile-device browser matrix remain explicit manual boundaries. Persisted chat still fully hydrates into Zustand; off-window component-local UI state can reset; `first_char_delay_*` and module-level image caches still lack central eviction.
- `docs/superpowers/plans/` is tracked for cross-device collaboration and handoff. A focused sensitive-information audit found no literal credentials, private keys, common token formats, personal home paths, or email addresses; future updates must preserve that boundary. Root `AGENTS.md` and `CLAUDE.md` remain local-only through `.git/info/exclude`.

- Current completed slice is the rejected-acceptance follow-up to `3dbb00ff`. The deployed failure was not a Jimeng service outage: the LLM produced a closed `json:mcp:jimeng-mcp` fence whose JSON ended with `"poll":0}}` instead of `"poll":0}}}`. Streaming showed Preparing, strict parsing then failed, the render projection dropped the internal tool message, and the store catch persisted no visible terminal state. Jimeng now performs one tightly bounded recovery only for a missing outer root `}`; all other malformed, incomplete, generic, or multiple MCP requests remain strict failures and never execute. Store and render both preserve a localized terminal failure instead of a blank message or raw protocol.
- Final verification for the follow-up passed the complete Jest suite (`81/81` suites, `807/807` tests), zero-warning ESLint, TypeScript, `git diff --check`, production build, and independent review with no Critical/Important blocker. Chrome used the exact deployed payload in a development-only fixture at `1440x1024` and `390x844`: recoverable output stayed visible with optimized prompt/parameters/progress, malformed output showed one terminal failure, raw `json:mcp` text was absent, document overflow was zero, and Console warning/error was zero. No paid provider or new live Jimeng generation was performed; the deterministic store test proves the repaired request reaches `executeMcpAction` exactly once.
- The complete pre-merge audit starts from the fetched clean baseline `main=origin/main=merge-base=0280cb3d`, `dev=origin/dev=0e6972dd`: `dev` is 44 non-merge commits ahead, with 193 changed files, 30,576 insertions, and 6,403 deletions. The review traced the real UI -> store/persistence -> provider/API -> MCP/external-response chains and independently re-reviewed the final worktree.
- Confirmed fixes close provider false-success and raw-error paths, settle-once streaming/timeout cleanup, active abort misclassification and lost late Responses traces, image-cancel fake progress, MCP access-tier gaps and wrong-session result routing, sync prototype pollution/mutable snapshots, clear-data pending-write resurrection, unstable long-history render keys, false server-lock presentation, optimistic unknown-model capabilities, and smaller Markdown/rail/artifact/i18n regressions.
- Final verification includes the complete Jest suite (`80/80` suites, `788/788` tests), zero-warning ESLint, TypeScript, diff checks, production build, 21 independent targeted suites / 293 tests, and real Browser QA at `1440x1024`, `1056x834`, `390x844`, and `320x740` across auto/Light/Dark and Chinese/English. `yarn export` remains blocked by the dynamic image POST route already present on `main`; Mermaid and OS file-picker behavior use deterministic test evidence rather than false live claims.
- Delivery is one commit pushed only to `origin/dev`. No merge, rebase, history rewrite, PR, deploy, remote configuration write, or intentional paid-provider request is included. Root `AGENTS.md`, `.env`, caches, screenshots, and build products remain excluded; the tracked current/iteration handoff ships with the product fix.
- Product commit `6d1ab3a6` supersedes the composer portion of `0900ffff`: mobile/tablet composer expansion is now keyed only by the persistent `collapsed` state, so entering text cannot remove the `24px` card geometry; compact layout now also considers the chat width left after the persisted sidebar, switching the sidebar to its drawer layout before the desktop composer becomes unreadably narrow. Chat/provider/store/persistence semantics, the collapsed pill, and the prior table-drag fix are unchanged. No push, PR, deploy, credential entry, or remote write was performed.
- Branch: `dev`.
- Product commit `0900ffff` fixes two frontend regressions without changing chat/provider/store semantics: Markdown wide-table dragging now uses one custom `role="slider"` whose pointer position is mapped to the actual thumb travel with grab-offset preservation and Pointer Capture cleanup; the mobile empty composer now switches from the compact `999px` pill to a stable `24px` card only after it expands. PageUp/PageDown follow ARIA slider increment/decrement semantics. No push, PR, deploy, credential entry, or remote write was performed.
- Product commit `aa2172a0` removes the new-chat homepage suggestion cards from desktop and mobile. The shared card JSX, click-to-fill handler, responsive/theme/accessibility SCSS, Chinese/English card copy, and obsolete visual contracts are deleted; `Chat / Image`, title, composer, model controls, request/state/persistence behavior, and existing conversations remain unchanged. No push, PR, or deploy was performed.
- The final pre-merge audit starts from the fetched and clean baseline `main=origin/main=merge-base=0280cb3d`, `dev=origin/dev=0a74915b`: `dev` is 35 non-merge commits ahead, with 183 changed files, 27,739 insertions, and 5,511 deletions. Review coverage followed the real UI -> store/persistence -> provider/API -> MCP/external-response chains rather than only the latest commit or prior handoff claims.
- Confirmed fixes close public upstream-error leakage, clear-data/pending-write resurrection, stale IndexedDB fallback reads, mixed healthy/broken MCP initialization, retry recovery, async content-growth tail following, PDF worker cleanup, HTML preview reload identity, OpenAI model capability/output/streaming mismatches, Summary output-budget trimming, conservative unknown-image options, and duplicate message-image keys. Independent semantic re-review found no remaining Critical or Important issue in the changed scope.
- Final verification passed the full Jest suite (`77/77` suites, `725/725` tests), zero-warning ESLint, TypeScript, `git diff --check`, production `yarn build`, targeted OpenAI/MCP/persistence/PDF/artifact/scroll suites, and real Browser QA at `1440x1024`, `390x844`, and `320x740` in Light/Dark with zero horizontal overflow and a clean fresh console. `yarn export` remains blocked by the pre-existing `/api/openai/v1/images/[action]` POST route under `output: export`; that route and `next.config.mjs` are identical to `main`, so this audit does not disguise a server API as a static artifact.
- The delivery boundary is one commit containing this handoff and the audit fixes, pushed only to `origin/dev`. No merge, rebase, history rewrite, PR, deploy, credential entry, paid request, remote configuration change, or write to `main` is included.
- The new-chat home defaults to a real available GPT-5.x Chat model and exposes `Chat / Image` tabs. Chat shows only reasoning effort; Image hides model copy, collapses auto size/quality to one `Auto / 自动` summary, and opens directly into unified size/quality rails. Existing GPT/Image conversations also open their current parameter rail first and retain Back navigation to model selection. No push, PR, deploy, paid model request, or remote configuration change was performed.
- GPT Image options are now resolved by model and shared by composer, Settings, generation JSON, and edit multipart requests. GPT Image 2 exposes only `auto/low/medium/high` quality and `auto` plus seven popular sizes; stale cross-model values are normalized before display/request construction; size meanings are localized in English and Chinese while preserving exact API values. Product auth, provider gates, locks, persistence format, custom-model compatibility, and deployment remain unchanged.
- The prior composer-only model selection and approved GPT-5.x reasoning rail remain committed in `5231fc17` and are reused by this slice.
- Root `AGENTS.md` remains local-only. Tracked `docs/superpowers/plans/current.md` and the routed dated iteration are committed with the product slice they describe.

## Task Routing Boundary

Recent commits contain substantial Gemini-style UI/UX work, but that history is not the default scope for new tasks. Route each request from its actual layer:

- Frontend/UI and Markdown
- Backend/API and provider/streaming
- MCP/Plugin and external integrations
- Auth/security
- State, persistence, sync, import/export
- Configuration, deployment, export, or Tauri desktop
- Cross-layer request chains

Do not turn backend, runtime, configuration, or persistence work into a visual-polish slice. Do not apply Browser QA to server-only changes without a UI surface.

## Stable Source Anchors

- Frontend shell and interaction: `app/components/`, `app/styles/`.
- Backend/API and security: `app/api/`, `app/utils/access-*.ts`.
- Provider client and streaming: `app/client/`, `app/utils/stream.ts`, `app/utils/openai-*.ts`.
- MCP runtime and UI integration: `app/mcp/`, `app/components/mcp-market*`.
- State and persistence: `app/store/`, `app/utils/storage.ts`, `app/utils/indexedDB-storage.ts`, `app/utils/sync.ts`.
- Configuration and i18n: `app/config/`, `app/locales/`, `.env.template`, `next.config.mjs`.
- Deployment and desktop: `Dockerfile`, `docker-compose.yml`, `vercel.json`, `src-tauri/`.
- Contract tests: `test/`; choose suites by the changed layer.

## Recent Verified Changes

- 2026-07-16 Composer regression follow-up from baseline `3655af9`: Dark mode now normalizes legacy inline SVG paint only inside Composer-owned Tools, Prompt Library, attachment delete/file/full, and drag-drop surfaces, preserving shared assets and unrelated legacy filters. The open Model Chip no longer stretches its name lane into a blank gap, and textarea expansion now measures stable Compact and Expanded probe widths instead of feeding the current layout back into the next decision. Real Chrome verified every reachable icon state, a `4px` name/separator/detail gap, and monotonic mobile transitions (`390x844`: Compact through 16 characters then Expanded; `320x740`: Compact through 7 then Expanded) with zero alternating states or horizontal overflow. Final verification passed `87/87` Jest suites and `869/869` tests, zero-warning ESLint, TypeScript, `git diff --check`, and production build. No shared SVG asset, provider/store/persistence contract, dependency, push, PR, or deploy changed.
- 2026-07-13 Jimeng/image-gallery repair based on `e558c5bf`: scoped Chat-mode Jimeng requests now include canonical `json:mcp:jimeng-mcp`, `tools/call`, and real Dreamina schemas; stale Image-mode state cannot leak Jimeng MCP into `gpt-image-*` requests. Jimeng task output is projected into the same selected-main gallery as GPT Image multimodal output without changing stored/exported content. Desktop uses a right-side option rail; compact layouts use a bottom rail. Verification passed all `795` tests, ESLint, TypeScript, diff check, production build, and Light/Dark desktop/mobile Chrome QA. No live external generation, push, PR, or deploy was performed.
- 2026-07-13 complete `main...dev` audit at `dev=0e6972dd`: all 44 branch commits and the 193-file merge-base diff were reviewed across chat/streaming, every provider adapter, OpenAI Responses, MCP/auth, model/server config, Markdown/Mermaid, attachments/images, persistence/sync, Web/export/Tauri boundaries, i18n, accessibility, and security. The fixes and exact boundaries are routed through `iterations/2026-07-13-dev-main-final-premerge-review.md`; independent semantic review found no remaining Critical/High/Medium issue.
- 2026-07-13 responsive composer follow-up at `6d1ab3a6`: the mobile/tablet expanded selector is `.chat-input-panel:not(.chat-input-panel-collapsed)`, so typing no longer drops the card rule when the `empty` class disappears. `useCompactScreen` now combines viewport width with the effective persisted sidebar width and enters compact/drawer layout when the remaining chat lane is below `640px`; sidebar narrow/default/max normalization is shared in a pure helper. Verification passed 3 targeted suites / 95 tests, zero-warning ESLint, TypeScript, `git diff --check`, an equivalent standalone production build, and independent review. Real Chrome QA covered `1440x1024`, `1056x834`, `900px`, `800px`, `390x844`, and `320x740` across Light/Dark: non-empty compact composers kept `24px` radius, full controls, and zero horizontal overflow; wide desktop retained its `880px` composer. The deployed Vercel page was used only to reproduce the old failure; no push or deploy was performed.
- 2026-07-13 table-drag and mobile-composer regressions at `0900ffff`: the table control no longer layers a transparent native range over custom Pointer handlers. Its thumb uses real track-minus-thumb travel, preserves the grab offset, captures one pointer, clears cancel/lost-capture state, and removes position transitions that made the thumb chase the cursor. The mobile expanded-empty composer is selected by `.chat-input-panel-empty:not(.chat-input-panel-collapsed)`, so focus/blur does not toggle its `24px` card geometry while the collapsed `58px` composer retains the `999px` pill. Final verification passed 3 targeted suites / 96 tests, a 14-suite / 69-test focused Markdown/chat matrix, ESLint, TypeScript, `git diff --check`, production build, and independent re-review after correcting PageUp/PageDown semantics. Real Chrome QA covered regular and full-screen tables plus the composer at `1440x1024`, `1056x834`, `400x880`, `390x844`, and `320x740` across Light/Dark. Mouse press did not jump, forward/reverse drags were continuous, composer focus/blur stayed card-shaped, document overflow was zero, and the final Console/error overlay were clean.
- 2026-07-13 homepage suggestion-card removal at `aa2172a0`: desktop and mobile new-chat homes now render only the existing `Chat / Image` switch, welcome title, and composer. Source and SCSS contracts assert that `EmptySuggestions` and `.chat-empty-suggestion*` cannot return as hidden dead UI. Verification passed `test/gemini-visual-migration.test.ts` plus `test/chat-home-mode.test.ts` (`90/90`), ESLint, TypeScript, `git diff --check`, and production build. In-app Browser QA at `1440x1024` and `390x844` confirmed card DOM/copy `0`, horizontal overflow `0`, framework overlay `0`, clean Console, and working `Chat -> Image` switching on both viewports.
- 2026-07-13 Markdown wide-table reading at `9e9f42d0`: regular desktop geometry measured `920px` shell / `918px` viewport / `1277px` content with an always-visible `18px` slider; `390px` mobile measured `306px` shell / `304px` viewport / `1196px` content with a `20px` slider and zero document overflow. Full-screen desktop measured `1440px` dialog / `1416px` shell / `1414px` fitted viewport, showing every column and correctly removing the now-inert slider. Dialog open/close kept the page's three table DOM nodes unchanged, focused the close action, supported Escape and close-button exit, and restored focus to the opener. Browser QA covered `1440x1024`, `1056x834`, `390x844`, and `320x740` across Light/Dark. Verification passed 15 focused suites / 87 tests, the complete visual contract `84/84`, ESLint, TypeScript, `git diff --check`, production build, and independent semantic review. Browser Console retained two pre-existing `ChatInner` Zustand/render warnings whose stacks do not enter the Markdown table code; they are recorded as an unrelated non-claim rather than silently reported clean.
- 2026-07-13 final `main...dev` pre-merge audit: all 35 branch commits and the 183-file merge-base diff were reviewed across chat/streaming, OpenAI Responses and Chat Completions, provider boundaries, MCP, model/server config, Markdown/Mermaid, files/PDF, images, persistence, Web/export/Tauri boundaries, i18n, accessibility, and security. The audit fixes are documented in `iterations/2026-07-13-dev-main-final-premerge-review.md`; final local validation passed 77 suites / 725 tests, lint, TypeScript, diff checks, production build, independent review, and desktop/mobile Browser QA. Static export retains one pre-existing dynamic POST API incompatibility that is identical to `main`; paid providers, live third-party MCP, native Tauri packaging, Safari, and Firefox remain explicit non-claims.
- 2026-07-13 preset-prompt count correction at baseline `dev=571ee925`: desktop `PromptToast` and the mobile conversation-settings label now use `session.mask.context.length`, while the derived render `context` remains responsible only for the visible `BOT_HELLO` fallback. Empty-preset existing sessions therefore no longer show a false `1` after leaving the chat bottom, and real preset counts keep the existing primitive-prop path without adding a Zustand subscription. The regression contract failed before the implementation change and then passed; final verification passed the complete visual contract `84/84`, ESLint, TypeScript, `git diff --check`, production build (`103 kB / 189 kB`), and real Chrome at desktop constrained height plus `390x844`. Chrome showed `滚到最新` with zero false preset toast, mobile exposed plain `对话设置`, the empty preset modal opened correctly, and Console warning/error output was empty. No commit, push, PR, deploy, credential entry, paid request, provider/store/persistence change, or remote write was performed.
- 2026-07-13 Markdown anchor and chat quick-jump correction at baseline `dev=401f888e`: sanitized headings now receive message-scoped IDs plus local anchor keys, bare fragment links are resolved only inside their current `.markdown-body`, and clicks preserve the app's `#/chat` HashRouter URL while supporting percent-encoded Chinese, duplicate headings, and GFM footnote IDs. The floating quick-jump now tracks a 12px accumulated scroll direction, reaches the true earliest/latest virtual window, preserves streaming detach/follow semantics, and hides when its selected boundary is reached. File/media/app-route/external-link behavior, provider/store/persistence semantics, and the existing safe lane remain unchanged. Verification passed 11 focused suites / 47 tests, the complete visual contract `84/84`, ESLint, TypeScript, `git diff --check`, production build (`103 kB / 189 kB`), and real Chrome at desktop, `390x844`, and `320x740` with same-page anchor focus, dynamic `滚到最早/滚到最新`, correct boundary hiding, 0 horizontal overflow, and no product Console error; only Huaban extension hydration noise remained. No commit, push, PR, deploy, credential entry, or paid request was performed.
- 2026-07-13 model-menu visual acceptance correction: the prior generic Browser PASS is superseded for this state because it checked viewport containment, overflow, focus restoration, and Console output but did not assert the popover-to-composer spatial relationship. Live deployed measurements reproduced a `215px` desktop gap and `205px` mobile gap. The regression used a 420 px available `max-height` as the popup's actual height when computing `top`; restoring a bottom anchor keeps variable-height Reasoning, model-list, and Image panels attached at the intended `12px` desktop / `10px` compact gap. Local Chrome verification covered `1440x1024`, `1056x834`, `390x844`, and `320x740`, both above/below directions, content-height changes, zero horizontal overflow, and Escape focus restoration. The correction adds an executable geometry suite instead of relying only on source-string visual contracts. Final verification passed 67 suites / 608 tests, ESLint, TypeScript, `git diff --check`, production build (`103 kB / 189 kB`), and independent semantic review.
- 2026-07-13 comprehensive frontend follow-up: a real 140-message Markdown/code/table/image/details/attachment fixture kept the live DOM bounded to 45 messages and shifted bidirectionally with stable absolute indexes and scroll-anchor compensation. Optional Export, Image Editor, Realtime Chat, TTS, and provider discovery moved off the initial graph; Markdown images are lazy/async; streaming coalescing adapts only beyond 64K/128K characters; shortcut listeners and disabled preview projection no longer churn during each stream/input update. Mobile drawer motion preserves the existing 200 ms intent through `translate3d`, Reduced Motion/Transparency fallbacks remain, Settings is one 44px semantic target, Selector focus is trapped/restored, and the measured `320px` attachment input expanded from 30px to 96px without removing model/Send controls. Verification passed 66 suites / 604 tests, ESLint, TypeScript, `git diff --check`, production build, and real Browser QA at `1440x1024`, `1056x834`, `390x844`, and `320x740` across Light/Dark with zero horizontal overflow and zero Console warnings/errors. Physical Safari/Firefox/Edge/Windows High Contrast remain manual-test boundaries; persistence sharding and incremental Markdown parsing remain separate architectural work.
- 2026-07-13 `8768d70c`: the desktop-first frontend performance review preserves the reasoning rail's highest-level cyan-to-violet sparkle, but moves its continuous motion from `background-position` repainting to a pseudo-element `translate3d` animation with Reduced Motion support. The composer/menu now uses explicit focus/open classes instead of critical `:has()` selectors, updates placement on `visualViewport` resize/scroll, keeps the menu mounted for a real close transition, restores trigger focus, and exposes 44px Chat/Image, model, switch, and Send targets. Markdown ordinary spans no longer mount formula measurement hooks, streaming tables avoid recursive content serialization and width-state reset, and rail drags cache layout geometry. Final verification passed 65 suites / 597 tests, ESLint, TypeScript, `git diff` checks, production build, an independent reviewer, and real Chrome QA at `1440x1024`, `1056x834`, `390x844`, and `320x740` with zero horizontal overflow. Chrome Console contained only Huaban extension attribute-injection noise. The commit was pushed to `origin/dev`; no merge or deploy was performed.
- 2026-07-12 `main...dev` pre-merge review closed eleven findings: Jimeng paused activation now uses an ephemeral runtime copy; PDF extraction is concurrency-bounded and order-stable; Jimeng prompt parsing uses the final progress marker; model testing preserves the authenticated effective key; all Responses streams enforce failed/incomplete/error terminals; Plugin execution and the Responses loop have finite abort-safe deadlines; shared SSE timeout/Stop paths settle once and clean their current timer; pending unknown-outcome recovery suppresses direct side-effect tools for the recovery turn; unavailable/locked home modes are native-disabled; Dark highest reasoning effort meets 4.5:1; and virtual rows expose full-set ARIA position. Full verification passed 65 suites / 594 tests, ESLint, TypeScript, production build, `git diff --check`, and an independent semantic review. Real Chrome QA covered `1440x1024` and `390x844`, Chat/Image panels, focus restoration, no horizontal overflow, and current Console evidence. No real third-party side effect, credential, merge, PR, or deploy was performed.
- 2026-07-12 final model release review covered every commit from `6c8760f7` through the final tree. It preserves the effective environment default during empty-session hydration, separates every ordinary chat model from supported OpenAI/Azure image models, exposes DALL-E in Image mode, and keeps unsupported image-family names out of Chat. Model-specific reasoning defaults no longer create an implicit global allowlist. GPT-5/5.1/5.2/5.4/5.5/5.6 and Pro variants use current effort matrices, including localized `minimal`; invalid OpenAI GPT cross-model overrides now reconcile value, source metadata, and derived token budgets without changing Anthropic/Azure state. Max output limits are `128K` for current GPT-5 base families and 5.4/5.5 Pro, `272K` for GPT-5 Pro, and `16,384` for GPT-5/5.1/5.2 `chat-latest`; non-GPT-5 fallback remains `512K`. The full suite passed `62/62` suites and `580/580` tests, ESLint, TypeScript, `git diff --check`, and production build. Real Chrome QA passed desktop, `390x844`, and `320x740` with Luna default, centered parameter controls, a header-free model list, visible switch action, and zero horizontal overflow. Independent semantic re-review found no remaining Critical or Important issue. No remote configuration, credential, paid request, push, PR, or deploy changed.
- 2026-07-12 model-selector regression follow-up: automatic empty-session normalization now reads the effective server default model/provider instead of hard-coding Terra, marks automatic writes as `server_default`/`fallback`, and keeps global-config sync enabled; explicit user selection remains a `conversation_override`. The expanded composer pill and parameter header center their model name, while the model-list page omits the duplicate current-model header. Local `/api/config` returned `gpt-5.6-luna@OpenAI`, and real Chrome showed Luna on a new chat, the centered parameter page, and a header-free Luna/Terra list. Five targeted suites passed `181/181`, ESLint, TypeScript, `git diff --check`, and production build passed. The deployed Vercel preview still reports Terra because its remote environment is configured differently; no remote configuration, push, PR, deploy, credential, or paid request was changed.

- 2026-07-12 explicit model switching fixes the actual navigation gap in the shared composer popup: empty GPT-5.x sessions can now leave the reasoning rail and reach the configured chat-model list, and existing sessions use the same labeled top-right action. The button reuses the existing monochrome circular-arrow asset, NeatChat surface/primary tokens, focus ring, and reduced-motion policy; provider selection, session/store writes, reasoning/image defaults, locks, persistence, requests, and Header actions are unchanged. Empty Chat no longer leaks `gpt-image-2` into its switch list.
- Verification passed the complete Gemini visual contract plus i18n (`105/105`), a focused component/behavior group (`120/120`), chat rendering/content and related contracts (`42/42`), ESLint, TypeScript, `git diff --check`, and production build. Real Chrome QA covered empty and existing sessions, `1650x941`, `390x844`, and `320x740` in the current Dark theme: the button remained within the popup, page overflow was zero, `Shift+Tab -> Enter` reached the model list, Escape removed the menu and restored focus to the composer trigger, and empty Chat listed exactly Luna/Terra. The supplied reference and implementation screenshot were inspected together; no P0/P1/P2 mismatch remained. No credential, paid request, push, PR, deploy, or remote state change occurred.
- 2026-07-12 per-model reasoning defaults extend `OPENAI_REASONING_EFFORT` from the legacy scalar to `*=effort;model=effort` syntax. Exact canonical model rules override `*`, `gpt-5.6` shares the Sol rule, missing rules fall back to `low`, and OpenAI/ChatGPT provider boundaries remain enforced. Public config preserves the effective scalar for old clients and additionally publishes the canonical map for model switches. Server-derived reasoning/max-token values follow the selected model, while user, conversation, locked, and administrator-forced overrides remain protected. `WEBUI_ALLOWED_REASONING_EFFORTS` remains a UI-only allowlist and is reconciled so each configured default stays visible.
- Verification passed 3 targeted suites / 75 tests, ESLint, TypeScript, `git diff --check`, production build, and an actual post-restart `/api/config` JSON 200. The live local `.env` resolved default `gpt-5.6-luna@OpenAI` to `xhigh` and published `*=medium`, Sol `medium`, Terra `high`, Luna `xhigh`. No credential, paid API call, push, PR, deploy, or remote state change was performed.
- Current HEAD centralizes OpenAI image model capabilities and normalizes stale cross-model size/quality values before every image UI/request boundary. GPT Image 2 no longer permits historical DALL-E `standard/hd` values to become extra rail stops; DALL-E 2/3 and Azure boundaries remain unchanged; known legacy GPT Image models use their smaller size set; unknown/custom GPT Image names retain prior request compatibility. English uses official size meanings and Chinese uses the requested `方形/横向/纵向 · 1K/1.5K/2K/4K` labels, with exact dimensions retained in Settings and common-size descriptions.
- Final verification for the current HEAD passed 5 targeted suites / 147 tests, TypeScript, ESLint, `git diff --check`, production build, independent semantic review, and real Chrome QA. After the documented post-build dev-server restart, `/api/config` returned JSON 200 and cold load showed 8 localized GPT Image 2 size stops plus exactly 4 quality stops with no `standard/hd`; product Console warn/error was zero. No image generation, credential entry, push, PR, deploy, or remote configuration change was performed.
- `349c0981` composer interaction correction passed 4 targeted suites / 114 tests, ESLint, TypeScript, `git diff --check`, production build, and Browser QA. The initial compiled-SCSS harness pass covered `1440x1024`, `390x844`, and `320x740` in Light/Dark: desktop menu placement was below with a 12px gap and zero card overlap; compact menus remained above and viewport-clamped; close/backdrop/Escape removed dialog and model copy in the next frame; pointer activation changed desktop `1→3` rows and mobile input height; the shared mode indicator preserved keyboard focus/selection; all viewports had zero horizontal overflow. The final input regression was verified directly in the user's authenticated Chrome route: click keeps the homepage center (`delta centerY = 0`) while changing the panel from `760x62` to `880x116`, the inner surface from `706x62 / 999px` to `826x116 / 30px`, and rows from `1` to `3`. The follow-up model-name audit then closed the remaining acceptance gap: closed new/old sessions have no model DOM or native title, while Desktop shows `model · detail` and `390/320px` compact open state shows the complete configured model name with parameter detail retained in the dialog. Chat/Image and new/existing paths measured `clientWidth = scrollWidth = 89px`, zero overflow, and no textarea/Send overlap. Every open dialog includes a wrapping current-model surface on Desktop and compact viewports, so arbitrary custom names remain fully readable after click without relying on a length heuristic or fixed trigger width. Console only contained Huaban extension attribute-injection noise, not a product error. Screenshots remain ignored under `test-results/composer-regression-qa/` and `test-results/composer-model-name-fix-qa/`; ignored `design-qa.md` records `final result: passed`.
- `7e99317d`: refines the shared composer selector for both empty and existing sessions. Empty Image no longer renders or announces a model row/name; `auto + auto` collapses to one localized `Auto / 自动`, and the popup contains only real capability-backed size/quality rails. Existing GPT-5.x and image sessions open their current parameter page first, with Back returning to the model list; reselecting the current model does not rewrite session defaults. The legacy image size/quality accordion/listbox path is removed. Rail stop dots are reduced to `4px` with `30%` light / `34%` dark opacity, while thumb, selected fill, locks, provider boundaries, and request/store contracts remain unchanged.
- Verification for `7e99317d` passed 6 relevant Jest suites / 127 tests, ESLint, TypeScript, `git diff --check`, production build, and independent read-only semantic review. Real in-app Browser QA covered `1440x1024`, `390x844`, `320x740`, dark/light, empty Chat/Image, existing GPT/Image, Back, Escape focus restoration, zero horizontal overflow, and a clean post-restart console. The known post-build stale CSS chunk was cleared by restarting the dev server before repeating the real route; no product workaround was added. Reference and implementation were inspected together in the ignored comparison board, and ignored `design-qa.md` records `final result: passed`.
- `dfda494e`: adds localized `Chat / Image` (`聊天 / 生图`) tabs to the empty new-chat home. Mode is projected from the real session model/provider; unsupported or image global defaults normalize once per new temporary session to an available GPT-5.x unless administrator-locked. Chat opens reasoning directly. Image prefers `gpt-image-2`, filters to available OpenAI image models, and renders image size/quality through the generic discrete rail extracted from `ReasoningEffortRail`. Tabs have roving keyboard control and a linked tabpanel; Image uses a distinct localized dialog name. Existing conversations retain the previous all-model and image-option branches.
- Verification for `dfda494e` passed 6 relevant Jest suites / 136 tests, complete Gemini visual contract `84/84`, post-commit focused 5 suites / 52 tests, ESLint, TypeScript, `git diff --check`, production build, and independent read-only re-review. Real in-app Browser QA covered `1440x1024`, `390x844`, and `320x740`: default Chat, Image `gpt-image-2`, reasoning/image rail keyboard updates, tab Arrow navigation, tabpanel/dialog ARIA, zero horizontal overflow, and a clean final console. Reference and implementation were reviewed in one ignored comparison board; `design-qa.md` is `PASSED` for this skill-required run.
- `5231fc17`: implements the approved drill-in reasoning rail inside the single composer model selector. The trigger visibly shows `model · current detail · caret`, grows left without moving Send, and remains visible for unsent drafts. GPT-5.x enters a localized horizontal rail with canonical dynamic stops, current-only history values, locked feedback, single-stop read-only behavior, pointer capture, Arrow/Home/End keyboard control, Back/Escape focus restoration, reduced-motion handling, and highest-available cyan-to-violet sparkle. Image models keep their size/quality branch; no fast-mode/lightning affordance was added. Popup placement is composer-aware, prefers above, flips below, uses `380px` desktop / `min(360px, viewport-24px)` mobile widths, and clamps its height/edges.
- Final verification passed 7 relevant Jest suites / 167 tests, ESLint, TypeScript, `git diff --check`, production build, and independent read-only review. The local real Chat route remained access-code gated and was not bypassed. In-app Browser QA used a temporary compiled-product-style interactive fixture at `1440x1024`, `390x844`, and `320x740`: Light `中`, Dark `极高`, real pointer drag, keyboard End, current-only, locked, single option, image branch, long unsent draft, zero console warnings/errors, and zero horizontal overflow. Browser-led fixes removed a double-card rail shell, anchored mobile vertical placement to the whole composer to preserve a 10px gap, centered mobile popup on the composer axis, reserved at least an 88px narrow input lane, and retained dialog focus when switching pointer-to-keyboard. The fixture and compiled CSS were deleted; screenshots remain local-only under ignored `test-results/model-selector-qa/`.
- 2026-07-11 composer model-selector unification removes the desktop and mobile Header selector entries, keeps Header conversation actions intact, and renders one selector immediately before Send in every composer state. The prior unsent-draft gap came from gating the composer selector on `showEmptyComposer`; that gate is removed. Model/provider updates, GPT-5.x reasoning allowlists, image defaults, store/persistence semantics, keyboard navigation, Escape/backdrop close, and focus restoration remain on the existing shared menu path. Responsive padding reserves a non-overlapping model lane down to `320x740`.
- Verification passed the complete Gemini visual contract (`84/84`), chat rendering (`6/6`), ESLint, TypeScript, `git diff --check`, and production build. The legitimate local Chat route remained access-code gated and was not bypassed. Chrome used a temporary static fixture compiled from the final SCSS at `1440x1024`, `390x844`, and `320x740`; empty, unsent-draft, and attachment-expanded composers kept one model control without input/Send overlap, and the final `320px` page overflow was zero. The fixture and compiled CSS were deleted. One console entry came from the Huaban Chrome extension injecting an extra HTML attribute on the access gate, not from the fixture or product code.
- The GPT-5.x reasoning visual design gate is closed by `5231fc17`. The final responsive component uses the approved Light desktop `中`, Dark desktop `极高`, and Mobile `中` state set while preserving existing wire values, allowlist filtering, locked-state and session/store semantics.
- 2026-07-11 reasoning-effort UI allowlist adds `WEBUI_ALLOWED_REASONING_EFFORTS` as a public UI policy. The old global CSV syntax remains supported; scoped syntax uses `*=...;model=...`, with exact model rules overriding the global fallback and `gpt-5.6` canonicalized to `gpt-5.6-sol`. Settings, the composer selector, and the header model menu intersect the selected policy with the model's API-supported efforts. The default model retains `OPENAI_REASONING_EFFORT` through an exact model override without widening other models. Existing conversation overrides outside the list remain truthful current-only disabled entries. Chinese labels are now `快速/低/中/高/极高/MAX`; `light` and product-level `Ultra` are not added as Responses API wire values.
- `b7a56703`: data surfaces now default to the centered `780px` prose rail and promote monotonically to the centered `920px` rail only after real table/code/SVG/iframe metrics prove they need it. Tables default to logical start alignment while preserving GFM inline alignment, expose overflow-only labeled regions and a dismissible mobile hint, convert only strict dash-plus-`br` cells into semantic lists, and avoid duplicate final-row borders. The same slice adds neutral row hover vs keyboard focus, attached artifact chrome without restyling iframe content, compact consecutive separators, and a real `66px`/`60px` scroll-button safe lane with a desktop gutter.
- Post-commit verification passed `15` relevant suites / `159` tests, a focused post-hook rerun of `6` suites / `107` tests, ESLint, TypeScript, `git diff --check`, independent review, and a production build. Local Chat remained access-code gated and was not bypassed. Chrome loaded the real compiled product CSS in a local-only fixture at `1650x941`, `1056x834`, `390x844`, and `320x740` across Light/Dark: normal/wide rails were `780/920px` when space allowed, collapsed to the same centered mobile width, table overflow stayed internal, safe-lane geometry was `42+12+12` or `40+10+10`, artifact caption/frame were attached, and page overflow was zero. A fresh fixture tab and the already-open authenticated NeatChat tab both had zero console warning/error.
- 2026-07-11 homepage composer correction decouples opening the conversation-tools menu from `expandInput()`, so the empty composer keeps its compact pill geometry while the menu opens. The textarea now explicitly overrides the global `*:focus-visible` outline; focus remains visible on the rounded composer surface instead of rendering a rectangular native outline. The visual contract passed `84/84`, ESLint, TypeScript, `git diff --check`, and production build passed. Auth blocked the real local chat route, so Chrome QA used a temporary compiled-SCSS fixture without bypassing access control: composer stayed `706x62px` with `999px` radius before/after menu open, menu width was `320px`, textarea `outline-style` was `none`, and there was no horizontal overflow. The fixture was deleted; no push or deploy was performed.
- 2026-07-11 Markdown acceptance correction fixes five user-visible regressions from the three-slice version: all prose/blockquote/hr/details/task/math roots now resolve onto the centered `780px` track; Markdown task checkboxes suppress the global checked `::after` and use one `18px` control; sanitizer preserves remark-math `span.math.math-inline` and `div.math.math-display` markers so KaTeX runs; generic details child padding no longer erases list/code gutter padding; and desktop prompt/actions share a right-side cluster with actions last.
- Fresh verification passed the real sanitize-to-KaTeX runtime (`12/12`), rejected-layout contracts (`4/4`), relevant Markdown fixture contracts, Chat rendering, the complete visual suite (`84/84` before the fixture-only extension plus final targeted rerun), ESLint, TypeScript, `git diff --check`, and a production build. Chrome `1650x941` Light/Dark QA used the actual Markdown component and production Header classes without bypassing the access gate: canvas `920px`, prose/details/formulas `780px`, checkbox `18px` with `::after=none`, KaTeX inline/display `1/3`, details code start padding `58px`, Header prompt-to-actions gap `10px`, actions right gap `18px`, and no horizontal overflow. The temporary QA route/cache were deleted.
- 2026-07-11 uncommitted Markdown optimization was delivered as three independently observable slices: semantic/accessibility hardening (native closed/open details, overflow-only table/formula regions, media semantics, focus/pinch behavior); a 780px prose / 920px data reading system with 15px default text and calmer light/dark surfaces; and long-form interactions (non-overlay contextual prompt slot plus mobile badge, code action overlay without reserved content rail, static no-latency token metadata, Mermaid figure/source disclosure, and source-validated uniquely titled HTML previews).
- Verification passed `84` visual-contract tests, `31` targeted behavior tests across Markdown/Sanitize/Mermaid/chat/artifact preview, ESLint, TypeScript, `git diff --check`, and a production build. Browser QA used a temporary local compiled-CSS fixture because the legitimate chat route was access-code gated; desktop `1440x1024`, mobile `390x844`, and narrow `320x740` had no page overflow, expected 780/920 widths, internal code scrolling, 34/36px code actions, closed details, dark theme tokens, and zero console warn/error. The fixture was deleted after QA.
- Independent read-only review found and closed two Important polish gaps before final verification: image preview/code-expand/token controls now keep a real high-contrast focus outline, and the mobile context badge uses an invariant white foreground with `99+` overflow text instead of the theme-dependent `--white` token.

- `095f97a5`: routed the Settings/chat/model/attachment/image/Markdown/editor/update-announcement UI introduced by the recent frontend work through typed Chinese and English locales; added an i18n contract; and made `reasoningMode`, `reasoningContext`, `inputImageDetail`, `promptCacheMode`, and `promptCacheKey` default administrator-locked fields without locking `reasoningEffort`. Global, persisted session, and temporary-session stale overrides are replaced by `admin_forced` values. The UI lock remains a client/public-config policy, not server-side request authorization.
- `cc6e8641`: added a GPT-5.6 Prompt Cache `disabled` setting. The internal mode maps to OpenAI's documented zero-write shape—`prompt_cache_options.mode="explicit"` with `ttl="30m"`, no `prompt_cache_key`, and no breakpoint—because omitting cache options would fall back to implicit caching. Settings expose Disabled/Implicit/Explicit and disable the key input while Disabled is selected. Local `.env` uses `disabled` with an empty key; no push or main merge was performed.
- `114c49fb`: added the OpenAI-only GPT-5.6 alias/Sol/Terra/Luna family, made `gpt-5.6-terra` the default, added the `2026-02-16` cutoff, six reasoning efforts, web/vision support, 128K output normalization, and migration of old/disallowed sessions.
- `524d338a`: added GPT-5.6 mode/context/image-detail/Prompt Cache settings and payloads, verified server-only `safety_identifier`, encrypted stored/stateless replay, native Responses Plugin function tools, finite/abort-safe recovery, Retry protection for side effects, and responsive Settings controls.
- `114c49fb` and `524d338a` were fast-forwarded from local `main` to `dev` and pushed to `origin/dev`. Post-merge verification passed all 37 Jest suites and 375 tests, lint, `tsc`, diff checks, and the production build.
- `3ca4a7c8`: MCP initialization now retries retryable transient network failures with finite backoff and records an error only after final failure. Server-side verification used targeted Jest, lint, `tsc`, diff check, and build; no Browser QA was required.
- `1e6dc330`: clipboard attachment collection deduplicates the same pasted file across browser clipboard representations; image editing replaces only the selected attachment.
- `0280cb3d`: duplicate attachment render keys and functional deletion updates stabilize removal of visually identical image/file attachments.
- Root `AGENTS.md` was rewritten on 2026-07-10 as an evidence-first full-stack guide. It now treats UI, API/provider, MCP, auth/security, state/persistence, configuration/deployment, and Tauri as first-class scopes and removes the obsolete claim that the visual migration test reads local `AGENTS.md`.

## Current Runtime And QA Boundaries

- The reasoning-effort policy and label correction passed 4 targeted Jest suites / 88 tests, focused ESLint, TypeScript, `git diff --check`, and production build. A production runtime with scoped configuration returned JSON 200 containing `default=[low]`, Sol `[high,xhigh,max]`, Terra `[none,low,medium]`, Luna `[low,medium]`, default model/effort `gpt-5.6-terra/medium`, and no reasoning lock. In-app Chat remained access-code gated and was not bypassed; the gate page had no browser console warning/error. The existing Chrome NeatChat tab targets the deployed dev site rather than the local final build, so it was not used as false visual evidence. UI behavior is covered by shared-helper tests, i18n contracts, and three-surface component contracts.
- The localization/admin-lock slice passed the complete Jest suite (`38` suites / `397` tests), lint, `tsc`, commit and range diff checks, and a fresh production build. In-app Browser QA at desktop `1280x720` and mobile `390x844` confirmed English Settings/chat chrome, all five GPT-5.6 controls disabled with `Administrator locked`, no horizontal overflow, one working conversation-tools interaction, and no console warnings/errors. User-authored chat titles and Custom Instructions remain untranslated by design.
- The older merged-state review from `081673c8` identified raw-HTML Markdown XSS and unknown-outcome Responses tool side effects. The current tree has sanitizer runtime coverage and finite unknown-outcome recovery with next-turn direct-tool gating; the 2026-07-12 `main...dev` re-review found no remaining Critical or Important issue.
- Prompt Cache Disabled verification passed 5 targeted Jest suites / 107 tests, lint, `tsc`, diff checks, and production build. Independent review caught and resolved the important distinction between omitted options (implicit default) and explicit-without-breakpoint (zero write). Browser QA confirmed Disabled/Implicit/Explicit, disabled key input, no overflow at desktop and `390x844`, and no console warning/error.
- GPT-5.6 verification used 18 relevant Jest suites (276 tests), lint, `tsc`, diff checks, production build, and in-app Browser QA at desktop `1280x720` and mobile `390x844`. Settings showed Terra plus all five capability controls, no horizontal overflow, and no browser console warnings/errors.
- No paid OpenAI request or live third-party Plugin execution was performed. External API behavior is covered by official schema review plus deterministic builder/SSE/proxy/executor tests; a live credentialed smoke test requires explicit authorization.
- Programmatic Tool Calling, hosted MCP, and Multi-agent remain intentionally disabled because they require separate beta headers, approval/caller protocols, and attribution semantics.
- Never bypass the access-code gate or enter real credentials. If authenticated Browser QA is blocked, use source/Jest, DOM/CSSOM, compiled CSS, deterministic fixtures, or server logs and state the boundary.
- In-app Browser screenshot, viewport control, or DOM snapshots can be unreliable; use current DOM metrics, console logs, CSSOM, and visible screenshots as cross-checks.
- Only in the known local post-build scenario, stale dev-server state can make `/api/config` return HTML/Edge errors; restart the dev server as a diagnostic branch, not as a general fix.
- Root `AGENTS.md` remains local-only. Tracked `docs/superpowers/plans/current.md` and its routed iteration belong to the product slice; do not recreate `design-qa.md` or a parallel `.agentdocs/` tree.

## Scoped Candidates

Use only when supported by a fresh request or current evidence:

- OpenAI GPT-5.6: only with explicit credentials/authorization, run a minimal live Responses smoke test for one text response and one harmless direct Plugin function. Do not enable PTC, hosted MCP, or Multi-agent as a payload-only follow-up.
- Backend/API: if real image requests still exceed Vercel's current function limit, evaluate provider-supported background/streaming flow or a runtime with a longer execution window.
- MCP/runtime: Jimeng is no longer part of the product. Keep generic MCP schema validation and confirmation boundaries; do not restore a provider-specific image control plane without a new product decision.
- Frontend/UI: continue Gemini/Markdown polish only from a fresh visible regression; do not reopen covered surfaces from old screenshots alone.
- Security: retain the current raw-HTML sanitizer runtime contract and expand it only when new Markdown elements or attributes are intentionally allowed.
- Responses tools: retain unknown-outcome markers, one-turn direct-tool gating, finite deadlines, and settle-once behavior; any future automatic replay of mutating tools requires an explicit idempotency contract or user confirmation.
- Localization: the recent UI chrome is covered, but the legacy Word/PowerPoint/PDF/ZIP/Excel reader error dialogs in `app/utils/file.ts` still contain Chinese and need a separate parser-output/i18n contract before broad translation.
- State/persistence/auth/deployment/Tauri: no standing rewrite is assumed; begin from live code, current config, runtime evidence, and the user's requested outcome.
