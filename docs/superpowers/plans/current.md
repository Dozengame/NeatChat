# Current Plan State

Last updated: 2026-07-13.

## Repo State

- Branch: `dev`.
- Current head `9e9f42d0` fixes Markdown ultra-wide table reading: only genuinely overflowing tables receive a reading toolbar, a persistent OS-independent horizontal slider, and an in-app full-screen dialog. The implementation keeps one table DOM, preserves adaptive `780/920px` promotion and scroll position, restores trigger focus after Escape/close, uses the full dialog width when desktop space can reveal every column, and hides the slider when full-screen no longer overflows. No provider/API/MCP/auth/store/persistence/deployment behavior changed; no push, PR, or deploy was performed.
- The final pre-merge audit starts from the fetched and clean baseline `main=origin/main=merge-base=0280cb3d`, `dev=origin/dev=0a74915b`: `dev` is 35 non-merge commits ahead, with 183 changed files, 27,739 insertions, and 5,511 deletions. Review coverage followed the real UI -> store/persistence -> provider/API -> MCP/external-response chains rather than only the latest commit or prior handoff claims.
- Confirmed fixes close public upstream-error leakage, clear-data/pending-write resurrection, stale IndexedDB fallback reads, mixed healthy/broken MCP initialization, retry recovery, async content-growth tail following, PDF worker cleanup, HTML preview reload identity, OpenAI model capability/output/streaming mismatches, Summary output-budget trimming, conservative unknown-image options, and duplicate message-image keys. Independent semantic re-review found no remaining Critical or Important issue in the changed scope.
- Final verification passed the full Jest suite (`77/77` suites, `725/725` tests), zero-warning ESLint, TypeScript, `git diff --check`, production `yarn build`, targeted OpenAI/MCP/persistence/PDF/artifact/scroll suites, and real Browser QA at `1440x1024`, `390x844`, and `320x740` in Light/Dark with zero horizontal overflow and a clean fresh console. `yarn export` remains blocked by the pre-existing `/api/openai/v1/images/[action]` POST route under `output: export`; that route and `next.config.mjs` are identical to `main`, so this audit does not disguise a server API as a static artifact.
- The delivery boundary is one commit containing this handoff and the audit fixes, pushed only to `origin/dev`. No merge, rebase, history rewrite, PR, deploy, credential entry, paid request, remote configuration change, or write to `main` is included.
- The new-chat home defaults to a real available GPT-5.x Chat model and exposes `Chat / Image` tabs. Chat shows only reasoning effort; Image hides model copy, collapses auto size/quality to one `Auto / 自动` summary, and opens directly into unified size/quality rails. Existing GPT/Image conversations also open their current parameter rail first and retain Back navigation to model selection. No push, PR, deploy, paid model request, or remote configuration change was performed.
- GPT Image options are now resolved by model and shared by composer, Settings, generation JSON, and edit multipart requests. GPT Image 2 exposes only `auto/low/medium/high` quality and `auto` plus seven popular sizes; stale cross-model values are normalized before display/request construction; size meanings are localized in English and Chinese while preserving exact API values. Product auth, provider gates, locks, persistence format, custom-model compatibility, and deployment remain unchanged.
- The prior composer-only model selection and approved GPT-5.x reasoning rail remain committed in `5231fc17` and are reused by this slice.
- Root `AGENTS.md` remains local-only. Tracked `docs/superpowers/plans/current.md` and the routed dated iteration are committed with the product slice they describe.

## Task Routing Boundary

Recent commits contain substantial Gemini-style UI/UX work, but that history is not the default scope for new tasks. Route each request from its actual layer:

- Frontend/UI and Markdown
- Backend/API and provider/streaming
- MCP/Plugin and external integrations
- Auth/security
- State, persistence, sync, import/export
- Configuration, deployment, export, or Tauri desktop
- Cross-layer request chains

Do not turn backend, runtime, configuration, or persistence work into a visual-polish slice. Do not apply Browser QA to server-only changes without a UI surface.

## Stable Source Anchors

- Frontend shell and interaction: `app/components/`, `app/styles/`.
- Backend/API and security: `app/api/`, `app/utils/access-*.ts`.
- Provider client and streaming: `app/client/`, `app/utils/stream.ts`, `app/utils/openai-*.ts`.
- MCP runtime and UI integration: `app/mcp/`, `app/components/mcp-market*`.
- State and persistence: `app/store/`, `app/utils/storage.ts`, `app/utils/indexedDB-storage.ts`, `app/utils/sync.ts`.
- Configuration and i18n: `app/config/`, `app/locales/`, `.env.template`, `next.config.mjs`.
- Deployment and desktop: `Dockerfile`, `docker-compose.yml`, `vercel.json`, `src-tauri/`.
- Contract tests: `test/`; choose suites by the changed layer.

## Recent Verified Changes

- 2026-07-13 Markdown wide-table reading at `9e9f42d0`: regular desktop geometry measured `920px` shell / `918px` viewport / `1277px` content with an always-visible `18px` slider; `390px` mobile measured `306px` shell / `304px` viewport / `1196px` content with a `20px` slider and zero document overflow. Full-screen desktop measured `1440px` dialog / `1416px` shell / `1414px` fitted viewport, showing every column and correctly removing the now-inert slider. Dialog open/close kept the page's three table DOM nodes unchanged, focused the close action, supported Escape and close-button exit, and restored focus to the opener. Browser QA covered `1440x1024`, `1056x834`, `390x844`, and `320x740` across Light/Dark. Verification passed 15 focused suites / 87 tests, the complete visual contract `84/84`, ESLint, TypeScript, `git diff --check`, production build, and independent semantic review. Browser Console retained two pre-existing `ChatInner` Zustand/render warnings whose stacks do not enter the Markdown table code; they are recorded as an unrelated non-claim rather than silently reported clean.
- 2026-07-13 final `main...dev` pre-merge audit: all 35 branch commits and the 183-file merge-base diff were reviewed across chat/streaming, OpenAI Responses and Chat Completions, provider boundaries, MCP, model/server config, Markdown/Mermaid, files/PDF, images, persistence, Web/export/Tauri boundaries, i18n, accessibility, and security. The audit fixes are documented in `iterations/2026-07-13-dev-main-final-premerge-review.md`; final local validation passed 77 suites / 725 tests, lint, TypeScript, diff checks, production build, independent review, and desktop/mobile Browser QA. Static export retains one pre-existing dynamic POST API incompatibility that is identical to `main`; paid providers, live third-party MCP, native Tauri packaging, Safari, and Firefox remain explicit non-claims.
- 2026-07-13 preset-prompt count correction at baseline `dev=571ee925`: desktop `PromptToast` and the mobile conversation-settings label now use `session.mask.context.length`, while the derived render `context` remains responsible only for the visible `BOT_HELLO` fallback. Empty-preset existing sessions therefore no longer show a false `1` after leaving the chat bottom, and real preset counts keep the existing primitive-prop path without adding a Zustand subscription. The regression contract failed before the implementation change and then passed; final verification passed the complete visual contract `84/84`, ESLint, TypeScript, `git diff --check`, production build (`103 kB / 189 kB`), and real Chrome at desktop constrained height plus `390x844`. Chrome showed `滚到最新` with zero false preset toast, mobile exposed plain `对话设置`, the empty preset modal opened correctly, and Console warning/error output was empty. No commit, push, PR, deploy, credential entry, paid request, provider/store/persistence change, or remote write was performed.
- 2026-07-13 Markdown anchor and chat quick-jump correction at baseline `dev=401f888e`: sanitized headings now receive message-scoped IDs plus local anchor keys, bare fragment links are resolved only inside their current `.markdown-body`, and clicks preserve the app's `#/chat` HashRouter URL while supporting percent-encoded Chinese, duplicate headings, and GFM footnote IDs. The floating quick-jump now tracks a 12px accumulated scroll direction, reaches the true earliest/latest virtual window, preserves streaming detach/follow semantics, and hides when its selected boundary is reached. File/media/app-route/external-link behavior, provider/store/persistence semantics, and the existing safe lane remain unchanged. Verification passed 11 focused suites / 47 tests, the complete visual contract `84/84`, ESLint, TypeScript, `git diff --check`, production build (`103 kB / 189 kB`), and real Chrome at desktop, `390x844`, and `320x740` with same-page anchor focus, dynamic `滚到最早/滚到最新`, correct boundary hiding, 0 horizontal overflow, and no product Console error; only Huaban extension hydration noise remained. No commit, push, PR, deploy, credential entry, or paid request was performed.
- 2026-07-13 model-menu visual acceptance correction: the prior generic Browser PASS is superseded for this state because it checked viewport containment, overflow, focus restoration, and Console output but did not assert the popover-to-composer spatial relationship. Live deployed measurements reproduced a `215px` desktop gap and `205px` mobile gap. The regression used a 420 px available `max-height` as the popup's actual height when computing `top`; restoring a bottom anchor keeps variable-height Reasoning, model-list, and Image panels attached at the intended `12px` desktop / `10px` compact gap. Local Chrome verification covered `1440x1024`, `1056x834`, `390x844`, and `320x740`, both above/below directions, content-height changes, zero horizontal overflow, and Escape focus restoration. The correction adds an executable geometry suite instead of relying only on source-string visual contracts. Final verification passed 67 suites / 608 tests, ESLint, TypeScript, `git diff --check`, production build (`103 kB / 189 kB`), and independent semantic review.
- 2026-07-13 comprehensive frontend follow-up: a real 140-message Markdown/code/table/image/details/attachment fixture kept the live DOM bounded to 45 messages and shifted bidirectionally with stable absolute indexes and scroll-anchor compensation. Optional Export, Image Editor, Realtime Chat, TTS, and provider discovery moved off the initial graph; Markdown images are lazy/async; streaming coalescing adapts only beyond 64K/128K characters; shortcut listeners and disabled preview projection no longer churn during each stream/input update. Mobile drawer motion preserves the existing 200 ms intent through `translate3d`, Reduced Motion/Transparency fallbacks remain, Settings is one 44px semantic target, Selector focus is trapped/restored, and the measured `320px` attachment input expanded from 30px to 96px without removing model/Send controls. Verification passed 66 suites / 604 tests, ESLint, TypeScript, `git diff --check`, production build, and real Browser QA at `1440x1024`, `1056x834`, `390x844`, and `320x740` across Light/Dark with zero horizontal overflow and zero Console warnings/errors. Physical Safari/Firefox/Edge/Windows High Contrast remain manual-test boundaries; persistence sharding and incremental Markdown parsing remain separate architectural work.
- 2026-07-13 `8768d70c`: the desktop-first frontend performance review preserves the reasoning rail's highest-level cyan-to-violet sparkle, but moves its continuous motion from `background-position` repainting to a pseudo-element `translate3d` animation with Reduced Motion support. The composer/menu now uses explicit focus/open classes instead of critical `:has()` selectors, updates placement on `visualViewport` resize/scroll, keeps the menu mounted for a real close transition, restores trigger focus, and exposes 44px Chat/Image, model, switch, and Send targets. Markdown ordinary spans no longer mount formula measurement hooks, streaming tables avoid recursive content serialization and width-state reset, and rail drags cache layout geometry. Final verification passed 65 suites / 597 tests, ESLint, TypeScript, `git diff` checks, production build, an independent reviewer, and real Chrome QA at `1440x1024`, `1056x834`, `390x844`, and `320x740` with zero horizontal overflow. Chrome Console contained only Huaban extension attribute-injection noise. The commit was pushed to `origin/dev`; no merge or deploy was performed.
- 2026-07-12 `main...dev` pre-merge review closed eleven findings: Jimeng paused activation now uses an ephemeral runtime copy; PDF extraction is concurrency-bounded and order-stable; Jimeng prompt parsing uses the final progress marker; model testing preserves the authenticated effective key; all Responses streams enforce failed/incomplete/error terminals; Plugin execution and the Responses loop have finite abort-safe deadlines; shared SSE timeout/Stop paths settle once and clean their current timer; pending unknown-outcome recovery suppresses direct side-effect tools for the recovery turn; unavailable/locked home modes are native-disabled; Dark highest reasoning effort meets 4.5:1; and virtual rows expose full-set ARIA position. Full verification passed 65 suites / 594 tests, ESLint, TypeScript, production build, `git diff --check`, and an independent semantic review. Real Chrome QA covered `1440x1024` and `390x844`, Chat/Image panels, focus restoration, no horizontal overflow, and current Console evidence. No real third-party side effect, credential, merge, PR, or deploy was performed.
- 2026-07-12 final model release review covered every commit from `6c8760f7` through the final tree. It preserves the effective environment default during empty-session hydration, separates every ordinary chat model from supported OpenAI/Azure image models, exposes DALL-E in Image mode, and keeps unsupported image-family names out of Chat. Model-specific reasoning defaults no longer create an implicit global allowlist. GPT-5/5.1/5.2/5.4/5.5/5.6 and Pro variants use current effort matrices, including localized `minimal`; invalid OpenAI GPT cross-model overrides now reconcile value, source metadata, and derived token budgets without changing Anthropic/Azure state. Max output limits are `128K` for current GPT-5 base families and 5.4/5.5 Pro, `272K` for GPT-5 Pro, and `16,384` for GPT-5/5.1/5.2 `chat-latest`; non-GPT-5 fallback remains `512K`. The full suite passed `62/62` suites and `580/580` tests, ESLint, TypeScript, `git diff --check`, and production build. Real Chrome QA passed desktop, `390x844`, and `320x740` with Luna default, centered parameter controls, a header-free model list, visible switch action, and zero horizontal overflow. Independent semantic re-review found no remaining Critical or Important issue. No remote configuration, credential, paid request, push, PR, or deploy changed.
- 2026-07-12 model-selector regression follow-up: automatic empty-session normalization now reads the effective server default model/provider instead of hard-coding Terra, marks automatic writes as `server_default`/`fallback`, and keeps global-config sync enabled; explicit user selection remains a `conversation_override`. The expanded composer pill and parameter header center their model name, while the model-list page omits the duplicate current-model header. Local `/api/config` returned `gpt-5.6-luna@OpenAI`, and real Chrome showed Luna on a new chat, the centered parameter page, and a header-free Luna/Terra list. Five targeted suites passed `181/181`, ESLint, TypeScript, `git diff --check`, and production build passed. The deployed Vercel preview still reports Terra because its remote environment is configured differently; no remote configuration, push, PR, deploy, credential, or paid request was changed.

- 2026-07-12 explicit model switching fixes the actual navigation gap in the shared composer popup: empty GPT-5.x sessions can now leave the reasoning rail and reach the configured chat-model list, and existing sessions use the same labeled top-right action. The button reuses the existing monochrome circular-arrow asset, NeatChat surface/primary tokens, focus ring, and reduced-motion policy; provider selection, session/store writes, reasoning/image defaults, locks, persistence, requests, and Header actions are unchanged. Empty Chat no longer leaks `gpt-image-2` into its switch list.
- Verification passed the complete Gemini visual contract plus i18n (`105/105`), a focused component/behavior group (`120/120`), chat rendering/content and related contracts (`42/42`), ESLint, TypeScript, `git diff --check`, and production build. Real Chrome QA covered empty and existing sessions, `1650x941`, `390x844`, and `320x740` in the current Dark theme: the button remained within the popup, page overflow was zero, `Shift+Tab -> Enter` reached the model list, Escape removed the menu and restored focus to the composer trigger, and empty Chat listed exactly Luna/Terra. The supplied reference and implementation screenshot were inspected together; no P0/P1/P2 mismatch remained. No credential, paid request, push, PR, deploy, or remote state change occurred.
- 2026-07-12 per-model reasoning defaults extend `OPENAI_REASONING_EFFORT` from the legacy scalar to `*=effort;model=effort` syntax. Exact canonical model rules override `*`, `gpt-5.6` shares the Sol rule, missing rules fall back to `low`, and OpenAI/ChatGPT provider boundaries remain enforced. Public config preserves the effective scalar for old clients and additionally publishes the canonical map for model switches. Server-derived reasoning/max-token values follow the selected model, while user, conversation, locked, and administrator-forced overrides remain protected. `WEBUI_ALLOWED_REASONING_EFFORTS` remains a UI-only allowlist and is reconciled so each configured default stays visible.
- Verification passed 3 targeted suites / 75 tests, ESLint, TypeScript, `git diff --check`, production build, and an actual post-restart `/api/config` JSON 200. The live local `.env` resolved default `gpt-5.6-luna@OpenAI` to `xhigh` and published `*=medium`, Sol `medium`, Terra `high`, Luna `xhigh`. No credential, paid API call, push, PR, deploy, or remote state change was performed.
- Current HEAD centralizes OpenAI image model capabilities and normalizes stale cross-model size/quality values before every image UI/request boundary. GPT Image 2 no longer permits historical DALL-E `standard/hd` values to become extra rail stops; DALL-E 2/3 and Azure boundaries remain unchanged; known legacy GPT Image models use their smaller size set; unknown/custom GPT Image names retain prior request compatibility. English uses official size meanings and Chinese uses the requested `方形/横向/纵向 · 1K/1.5K/2K/4K` labels, with exact dimensions retained in Settings and common-size descriptions.
- Final verification for the current HEAD passed 5 targeted suites / 147 tests, TypeScript, ESLint, `git diff --check`, production build, independent semantic review, and real Chrome QA. After the documented post-build dev-server restart, `/api/config` returned JSON 200 and cold load showed 8 localized GPT Image 2 size stops plus exactly 4 quality stops with no `standard/hd`; product Console warn/error was zero. No image generation, credential entry, push, PR, deploy, or remote configuration change was performed.
- `349c0981` composer interaction correction passed 4 targeted suites / 114 tests, ESLint, TypeScript, `git diff --check`, production build, and Browser QA. The initial compiled-SCSS harness pass covered `1440x1024`, `390x844`, and `320x740` in Light/Dark: desktop menu placement was below with a 12px gap and zero card overlap; compact menus remained above and viewport-clamped; close/backdrop/Escape removed dialog and model copy in the next frame; pointer activation changed desktop `1→3` rows and mobile input height; the shared mode indicator preserved keyboard focus/selection; all viewports had zero horizontal overflow. The final input regression was verified directly in the user's authenticated Chrome route: click keeps the homepage center (`delta centerY = 0`) while changing the panel from `760x62` to `880x116`, the inner surface from `706x62 / 999px` to `826x116 / 30px`, and rows from `1` to `3`. The follow-up model-name audit then closed the remaining acceptance gap: closed new/old sessions have no model DOM or native title, while Desktop shows `model · detail` and `390/320px` compact open state shows the complete configured model name with parameter detail retained in the dialog. Chat/Image and new/existing paths measured `clientWidth = scrollWidth = 89px`, zero overflow, and no textarea/Send overlap. Every open dialog includes a wrapping current-model surface on Desktop and compact viewports, so arbitrary custom names remain fully readable after click without relying on a length heuristic or fixed trigger width. Console only contained Huaban extension attribute-injection noise, not a product error. Screenshots remain ignored under `test-results/composer-regression-qa/` and `test-results/composer-model-name-fix-qa/`; ignored `design-qa.md` records `final result: passed`.
- `7e99317d`: refines the shared composer selector for both empty and existing sessions. Empty Image no longer renders or announces a model row/name; `auto + auto` collapses to one localized `Auto / 自动`, and the popup contains only real capability-backed size/quality rails. Existing GPT-5.x and image sessions open their current parameter page first, with Back returning to the model list; reselecting the current model does not rewrite session defaults. The legacy image size/quality accordion/listbox path is removed. Rail stop dots are reduced to `4px` with `30%` light / `34%` dark opacity, while thumb, selected fill, locks, provider boundaries, and request/store contracts remain unchanged.
- Verification for `7e99317d` passed 6 relevant Jest suites / 127 tests, ESLint, TypeScript, `git diff --check`, production build, and independent read-only semantic review. Real in-app Browser QA covered `1440x1024`, `390x844`, `320x740`, dark/light, empty Chat/Image, existing GPT/Image, Back, Escape focus restoration, zero horizontal overflow, and a clean post-restart console. The known post-build stale CSS chunk was cleared by restarting the dev server before repeating the real route; no product workaround was added. Reference and implementation were inspected together in the ignored comparison board, and ignored `design-qa.md` records `final result: passed`.
- `dfda494e`: adds localized `Chat / Image` (`聊天 / 生图`) tabs to the empty new-chat home. Mode is projected from the real session model/provider; unsupported or image global defaults normalize once per new temporary session to an available GPT-5.x unless administrator-locked. Chat opens reasoning directly. Image prefers `gpt-image-2`, filters to available OpenAI image models, and renders image size/quality through the generic discrete rail extracted from `ReasoningEffortRail`. Tabs have roving keyboard control and a linked tabpanel; Image uses a distinct localized dialog name. Existing conversations retain the previous all-model and image-option branches.
- Verification for `dfda494e` passed 6 relevant Jest suites / 136 tests, complete Gemini visual contract `84/84`, post-commit focused 5 suites / 52 tests, ESLint, TypeScript, `git diff --check`, production build, and independent read-only re-review. Real in-app Browser QA covered `1440x1024`, `390x844`, and `320x740`: default Chat, Image `gpt-image-2`, reasoning/image rail keyboard updates, tab Arrow navigation, tabpanel/dialog ARIA, zero horizontal overflow, and a clean final console. Reference and implementation were reviewed in one ignored comparison board; `design-qa.md` is `PASSED` for this skill-required run.
- `5231fc17`: implements the approved drill-in reasoning rail inside the single composer model selector. The trigger visibly shows `model · current detail · caret`, grows left without moving Send, and remains visible for unsent drafts. GPT-5.x enters a localized horizontal rail with canonical dynamic stops, current-only history values, locked feedback, single-stop read-only behavior, pointer capture, Arrow/Home/End keyboard control, Back/Escape focus restoration, reduced-motion handling, and highest-available cyan-to-violet sparkle. Image models keep their size/quality branch; no fast-mode/lightning affordance was added. Popup placement is composer-aware, prefers above, flips below, uses `380px` desktop / `min(360px, viewport-24px)` mobile widths, and clamps its height/edges.
- Final verification passed 7 relevant Jest suites / 167 tests, ESLint, TypeScript, `git diff --check`, production build, and independent read-only review. The local real Chat route remained access-code gated and was not bypassed. In-app Browser QA used a temporary compiled-product-style interactive fixture at `1440x1024`, `390x844`, and `320x740`: Light `中`, Dark `极高`, real pointer drag, keyboard End, current-only, locked, single option, image branch, long unsent draft, zero console warnings/errors, and zero horizontal overflow. Browser-led fixes removed a double-card rail shell, anchored mobile vertical placement to the whole composer to preserve a 10px gap, centered mobile popup on the composer axis, reserved at least an 88px narrow input lane, and retained dialog focus when switching pointer-to-keyboard. The fixture and compiled CSS were deleted; screenshots remain local-only under ignored `test-results/model-selector-qa/`.
- 2026-07-11 composer model-selector unification removes the desktop and mobile Header selector entries, keeps Header conversation actions intact, and renders one selector immediately before Send in every composer state. The prior unsent-draft gap came from gating the composer selector on `showEmptyComposer`; that gate is removed. Model/provider updates, GPT-5.x reasoning allowlists, image defaults, store/persistence semantics, keyboard navigation, Escape/backdrop close, and focus restoration remain on the existing shared menu path. Responsive padding reserves a non-overlapping model lane down to `320x740`.
- Verification passed the complete Gemini visual contract (`84/84`), chat rendering (`6/6`), ESLint, TypeScript, `git diff --check`, and production build. The legitimate local Chat route remained access-code gated and was not bypassed. Chrome used a temporary static fixture compiled from the final SCSS at `1440x1024`, `390x844`, and `320x740`; empty, unsent-draft, and attachment-expanded composers kept one model control without input/Send overlap, and the final `320px` page overflow was zero. The fixture and compiled CSS were deleted. One console entry came from the Huaban Chrome extension injecting an extra HTML attribute on the access gate, not from the fixture or product code.
- The GPT-5.x reasoning visual design gate is closed by `5231fc17`. The final responsive component uses the approved Light desktop `中`, Dark desktop `极高`, and Mobile `中` state set while preserving existing wire values, allowlist filtering, locked-state and session/store semantics.
- 2026-07-11 reasoning-effort UI allowlist adds `WEBUI_ALLOWED_REASONING_EFFORTS` as a public UI policy. The old global CSV syntax remains supported; scoped syntax uses `*=...;model=...`, with exact model rules overriding the global fallback and `gpt-5.6` canonicalized to `gpt-5.6-sol`. Settings, the composer selector, and the header model menu intersect the selected policy with the model's API-supported efforts. The default model retains `OPENAI_REASONING_EFFORT` through an exact model override without widening other models. Existing conversation overrides outside the list remain truthful current-only disabled entries. Chinese labels are now `快速/低/中/高/极高/MAX`; `light` and product-level `Ultra` are not added as Responses API wire values.
- `b7a56703`: data surfaces now default to the centered `780px` prose rail and promote monotonically to the centered `920px` rail only after real table/code/SVG/iframe metrics prove they need it. Tables default to logical start alignment while preserving GFM inline alignment, expose overflow-only labeled regions and a dismissible mobile hint, convert only strict dash-plus-`br` cells into semantic lists, and avoid duplicate final-row borders. The same slice adds neutral row hover vs keyboard focus, attached artifact chrome without restyling iframe content, compact consecutive separators, and a real `66px`/`60px` scroll-button safe lane with a desktop gutter.
- Post-commit verification passed `15` relevant suites / `159` tests, a focused post-hook rerun of `6` suites / `107` tests, ESLint, TypeScript, `git diff --check`, independent review, and a production build. Local Chat remained access-code gated and was not bypassed. Chrome loaded the real compiled product CSS in a local-only fixture at `1650x941`, `1056x834`, `390x844`, and `320x740` across Light/Dark: normal/wide rails were `780/920px` when space allowed, collapsed to the same centered mobile width, table overflow stayed internal, safe-lane geometry was `42+12+12` or `40+10+10`, artifact caption/frame were attached, and page overflow was zero. A fresh fixture tab and the already-open authenticated NeatChat tab both had zero console warning/error.
- 2026-07-11 homepage composer correction decouples opening the conversation-tools menu from `expandInput()`, so the empty composer keeps its compact pill geometry while the menu opens. The textarea now explicitly overrides the global `*:focus-visible` outline; focus remains visible on the rounded composer surface instead of rendering a rectangular native outline. The visual contract passed `84/84`, ESLint, TypeScript, `git diff --check`, and production build passed. Auth blocked the real local chat route, so Chrome QA used a temporary compiled-SCSS fixture without bypassing access control: composer stayed `706x62px` with `999px` radius before/after menu open, menu width was `320px`, textarea `outline-style` was `none`, and there was no horizontal overflow. The fixture was deleted; no push or deploy was performed.
- 2026-07-11 Markdown acceptance correction fixes five user-visible regressions from the three-slice version: all prose/blockquote/hr/details/task/math roots now resolve onto the centered `780px` track; Markdown task checkboxes suppress the global checked `::after` and use one `18px` control; sanitizer preserves remark-math `span.math.math-inline` and `div.math.math-display` markers so KaTeX runs; generic details child padding no longer erases list/code gutter padding; and desktop prompt/actions share a right-side cluster with actions last.
- Fresh verification passed the real sanitize-to-KaTeX runtime (`12/12`), rejected-layout contracts (`4/4`), relevant Markdown fixture contracts, Chat rendering, the complete visual suite (`84/84` before the fixture-only extension plus final targeted rerun), ESLint, TypeScript, `git diff --check`, and a production build. Chrome `1650x941` Light/Dark QA used the actual Markdown component and production Header classes without bypassing the access gate: canvas `920px`, prose/details/formulas `780px`, checkbox `18px` with `::after=none`, KaTeX inline/display `1/3`, details code start padding `58px`, Header prompt-to-actions gap `10px`, actions right gap `18px`, and no horizontal overflow. The temporary QA route/cache were deleted.
- 2026-07-11 uncommitted Markdown optimization was delivered as three independently observable slices: semantic/accessibility hardening (native closed/open details, overflow-only table/formula regions, media semantics, focus/pinch behavior); a 780px prose / 920px data reading system with 15px default text and calmer light/dark surfaces; and long-form interactions (non-overlay contextual prompt slot plus mobile badge, code action overlay without reserved content rail, static no-latency token metadata, Mermaid figure/source disclosure, and source-validated uniquely titled HTML previews).
- Verification passed `84` visual-contract tests, `31` targeted behavior tests across Markdown/Sanitize/Mermaid/chat/artifact preview, ESLint, TypeScript, `git diff --check`, and a production build. Browser QA used a temporary local compiled-CSS fixture because the legitimate chat route was access-code gated; desktop `1440x1024`, mobile `390x844`, and narrow `320x740` had no page overflow, expected 780/920 widths, internal code scrolling, 34/36px code actions, closed details, dark theme tokens, and zero console warn/error. The fixture was deleted after QA.
- Independent read-only review found and closed two Important polish gaps before final verification: image preview/code-expand/token controls now keep a real high-contrast focus outline, and the mobile context badge uses an invariant white foreground with `99+` overflow text instead of the theme-dependent `--white` token.

- `095f97a5`: routed the Settings/chat/model/attachment/image/Markdown/editor/update-announcement UI introduced by the recent frontend work through typed Chinese and English locales; added an i18n contract; and made `reasoningMode`, `reasoningContext`, `inputImageDetail`, `promptCacheMode`, and `promptCacheKey` default administrator-locked fields without locking `reasoningEffort`. Global, persisted session, and temporary-session stale overrides are replaced by `admin_forced` values. The UI lock remains a client/public-config policy, not server-side request authorization.
- `cc6e8641`: added a GPT-5.6 Prompt Cache `disabled` setting. The internal mode maps to OpenAI's documented zero-write shape—`prompt_cache_options.mode="explicit"` with `ttl="30m"`, no `prompt_cache_key`, and no breakpoint—because omitting cache options would fall back to implicit caching. Settings expose Disabled/Implicit/Explicit and disable the key input while Disabled is selected. Local `.env` uses `disabled` with an empty key; no push or main merge was performed.
- `114c49fb`: added the OpenAI-only GPT-5.6 alias/Sol/Terra/Luna family, made `gpt-5.6-terra` the default, added the `2026-02-16` cutoff, six reasoning efforts, web/vision support, 128K output normalization, and migration of old/disallowed sessions.
- `524d338a`: added GPT-5.6 mode/context/image-detail/Prompt Cache settings and payloads, verified server-only `safety_identifier`, encrypted stored/stateless replay, native Responses Plugin function tools, finite/abort-safe recovery, Retry protection for side effects, and responsive Settings controls.
- `114c49fb` and `524d338a` were fast-forwarded from local `main` to `dev` and pushed to `origin/dev`. Post-merge verification passed all 37 Jest suites and 375 tests, lint, `tsc`, diff checks, and the production build.
- `3ca4a7c8`: MCP initialization now retries retryable transient network failures with finite backoff and records an error only after final failure. Server-side verification used targeted Jest, lint, `tsc`, diff check, and build; no Browser QA was required.
- `1e6dc330`: clipboard attachment collection deduplicates the same pasted file across browser clipboard representations; image editing replaces only the selected attachment.
- `0280cb3d`: duplicate attachment render keys and functional deletion updates stabilize removal of visually identical image/file attachments.
- Root `AGENTS.md` was rewritten on 2026-07-10 as an evidence-first full-stack guide. It now treats UI, API/provider, MCP, auth/security, state/persistence, configuration/deployment, and Tauri as first-class scopes and removes the obsolete claim that the visual migration test reads local `AGENTS.md`.

## Current Runtime And QA Boundaries

- The reasoning-effort policy and label correction passed 4 targeted Jest suites / 88 tests, focused ESLint, TypeScript, `git diff --check`, and production build. A production runtime with scoped configuration returned JSON 200 containing `default=[low]`, Sol `[high,xhigh,max]`, Terra `[none,low,medium]`, Luna `[low,medium]`, default model/effort `gpt-5.6-terra/medium`, and no reasoning lock. In-app Chat remained access-code gated and was not bypassed; the gate page had no browser console warning/error. The existing Chrome NeatChat tab targets the deployed dev site rather than the local final build, so it was not used as false visual evidence. UI behavior is covered by shared-helper tests, i18n contracts, and three-surface component contracts.
- The localization/admin-lock slice passed the complete Jest suite (`38` suites / `397` tests), lint, `tsc`, commit and range diff checks, and a fresh production build. In-app Browser QA at desktop `1280x720` and mobile `390x844` confirmed English Settings/chat chrome, all five GPT-5.6 controls disabled with `Administrator locked`, no horizontal overflow, one working conversation-tools interaction, and no console warnings/errors. User-authored chat titles and Custom Instructions remain untranslated by design.
- The older merged-state review from `081673c8` identified raw-HTML Markdown XSS and unknown-outcome Responses tool side effects. The current tree has sanitizer runtime coverage and finite unknown-outcome recovery with next-turn direct-tool gating; the 2026-07-12 `main...dev` re-review found no remaining Critical or Important issue.
- Prompt Cache Disabled verification passed 5 targeted Jest suites / 107 tests, lint, `tsc`, diff checks, and production build. Independent review caught and resolved the important distinction between omitted options (implicit default) and explicit-without-breakpoint (zero write). Browser QA confirmed Disabled/Implicit/Explicit, disabled key input, no overflow at desktop and `390x844`, and no console warning/error.
- GPT-5.6 verification used 18 relevant Jest suites (276 tests), lint, `tsc`, diff checks, production build, and in-app Browser QA at desktop `1280x720` and mobile `390x844`. Settings showed Terra plus all five capability controls, no horizontal overflow, and no browser console warnings/errors.
- No paid OpenAI request or live third-party Plugin execution was performed. External API behavior is covered by official schema review plus deterministic builder/SSE/proxy/executor tests; a live credentialed smoke test requires explicit authorization.
- Programmatic Tool Calling, hosted MCP, and Multi-agent remain intentionally disabled because they require separate beta headers, approval/caller protocols, and attribution semantics.
- Never bypass the access-code gate or enter real credentials. If authenticated Browser QA is blocked, use source/Jest, DOM/CSSOM, compiled CSS, deterministic fixtures, or server logs and state the boundary.
- In-app Browser screenshot, viewport control, or DOM snapshots can be unreliable; use current DOM metrics, console logs, CSSOM, and visible screenshots as cross-checks.
- Only in the known local post-build scenario, stale dev-server state can make `/api/config` return HTML/Edge errors; restart the dev server as a diagnostic branch, not as a general fix.
- Root `AGENTS.md` and internal plans must remain out of product commits. Do not recreate `design-qa.md` or a parallel `.agentdocs/` tree.

## Scoped Candidates

Use only when supported by a fresh request or current evidence:

- OpenAI GPT-5.6: only with explicit credentials/authorization, run a minimal live Responses smoke test for one text response and one harmless direct Plugin function. Do not enable PTC, hosted MCP, or Multi-agent as a payload-only follow-up.
- Backend/API: if real image requests still exceed Vercel's current function limit, evaluate provider-supported background/streaming flow or a runtime with a longer execution window.
- MCP/runtime: if Jimeng activation still fails after retry, capture the current digest/server log and separate transient connect timeout from persistent reachability or remote-service failure.
- Frontend/UI: continue Gemini/Markdown polish only from a fresh visible regression; do not reopen covered surfaces from old screenshots alone.
- Security: retain the current raw-HTML sanitizer runtime contract and expand it only when new Markdown elements or attributes are intentionally allowed.
- Responses tools: retain unknown-outcome markers, one-turn direct-tool gating, finite deadlines, and settle-once behavior; any future automatic replay of mutating tools requires an explicit idempotency contract or user confirmation.
- Localization: the recent UI chrome is covered, but the legacy Word/PowerPoint/PDF/ZIP/Excel reader error dialogs in `app/utils/file.ts` still contain Chinese and need a separate parser-output/i18n contract before broad translation.
- State/persistence/auth/deployment/Tauri: no standing rewrite is assumed; begin from live code, current config, runtime evidence, and the user's requested outcome.
